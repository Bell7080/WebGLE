# 버전 기록

PuppetForge의 버전과 dev 로그. 최신 항목이 위에 온다.
업데이트할 때마다 여기에 버전과 변경 내용을 남긴다. (규칙은 `CLAUDE.md` 참고)

---

## v0.37.3 — 2026-08-18

**Phase**: 위치 고정 가중치와 공간 감쇠 보정

### 수정

- `위치 고정`의 고정 보정을 칠한 가중치와 관절 시작점으로부터의 거리에 함께 비례하도록 변경했다. 가중치가 1인 넓은 영역도 시작점은 바닥에 붙고 외곽은 부모·주변 움직임을 점차 받아 `완전 고정`처럼 통째로 굳지 않는다.
- 위치 고정 영역의 반경은 실제로 해당 관절이 칠해진 정점 범위에서 자동 계산하므로 새 설정값이나 저장 포맷 변경 없이 기존 작업에도 적용된다.

### 검증

- `npm test`, `npm run build`로 가중치 1인 발의 시작점·중간·외곽 감쇠, 기존 위치 고정 회전, 완전 고정 동작과 프로덕션 빌드를 확인한다.

### 참고

- 한 정점에만 위치 고정 가중치가 있는 경우에는 반경을 계산할 수 없어 그 정점을 시작점으로 간주해 완전히 고정한다.
- 테스트 전용 임시 코드, 실제 앱에 포함되지 않는 더미 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.37.2 — 2026-08-18

**Phase**: 애니메이션별 위치 고정 변형 보정

### 수정

- `위치 고정`은 관절 기준점만 원래 월드 좌표에 붙잡고 애니메이션의 회전과 크기 변형은 유지하도록 수정했다. 특정 애니메이션에서만 발을 위치 고정했을 때 `완전 고정`처럼 발 모양까지 굳는 문제를 해결했다.
- `완전 고정`은 이전과 같이 위치와 모양을 모두 고정하여 두 모드의 역할을 명확히 분리했다.

### 검증

- `npm test`, `npm run build`로 idle 애니메이션에서만 위치 고정한 발의 기준점, 발끝 회전, 완전 고정의 원형 유지와 프로덕션 빌드를 확인한다.

### 참고

- 저장 포맷은 변경하지 않았으며 기존 프로젝트를 다시 저장할 필요 없이 재생 시 수정된 변형이 적용된다.
- 테스트 전용 임시 코드, 실제 앱에 포함되지 않는 더미 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.37.1 — 2026-08-18

**Phase**: 영향 영역 외곽 클리핑 보정

### 수정

- 알파 마스크의 탐색 범위를 격자 반 칸과 고정 2px 여유 중 큰 값이 아니라 두 값을 더한 범위로 넓혔다. 격자 셀 모서리에 걸친 픽셀도 주변 정점의 변형에 포함되어 월드 위치에 남아 찢어져 보이지 않는다.
- 안티앨리어싱으로 알파가 아주 희미해진 픽셀도 작업물로 판정하도록 최소 알파 기준을 1로 낮췄다.

### 검증

- `npm test`, `npm run build`로 3000px 이미지와 384칸 격자에서 반 칸 바깥 2px 여유, 희미한 픽셀 판정, 프로덕션 빌드를 확인한다.

### 참고

- 외곽 여유는 영향 영역 편집과 자동 가중치의 클리핑에만 쓰이며 원본 이미지 픽셀이나 저장 포맷은 바꾸지 않는다.
- 테스트 전용 임시 코드, 실제 앱에 포함되지 않는 더미 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.37.0 — 2026-08-17

**Phase**: 영향 영역 다듬기와 영역 기반 채우기

### 추가

- `정리` 옆에 `다듬기` 버튼을 추가했다. 확인창 없이 누를 때마다 3×3 보간을 한 단계 적용해 영향 경계를 조금 넓고 부드럽게 만들며, 연속 클릭과 단계별 Undo를 지원한다.
- `정리`도 오점 제거와 빈 곳 채우기 뒤 같은 다듬기를 한 번 적용한다.

### 변경

- `모두 채우기`는 빈 정점을 관절점과의 거리로 나누지 않고, 이미 칠한 모든 영역에서 동시에 퍼지는 방식으로 가장 가까운 영역에 할당한다. 아무 영역도 없을 때만 기존 관절 거리 계산을 사용한다.
- 전체 보정 버튼 세 개를 기존 흑백 테마의 같은 크기 한 줄 버튼으로 정렬하고, 다듬기 문구와 결과를 10개 언어로 제공한다.

### 검증

- `npm test`, `npm run build`로 영역 기반 할당, 반복 다듬기 확장, 정리 결합, 다국어 상태 문구와 프로덕션 빌드를 확인한다.

### 참고

- 다듬기는 의도적으로 누적된다. 너무 많이 눌렀다면 각 클릭이 별도 기록이므로 `Ctrl+Z`로 원하는 단계까지 되돌릴 수 있다.
- 테스트 전용 임시 코드, 실제 앱에 포함되지 않는 더미 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.36.2 — 2026-08-17

**Phase**: 고밀도 격자 실시간 편집 최적화

### 변경

- 일반 격자를 128 · 192 · 256 · 384칸으로 조정했다. 500칸부터 수십만 정점과 삼각형 객체가 생기던 병목을 피하면서 기존 108칸보다 충분히 세밀한 범위를 유지한다.
- 브러시 한 획이 모든 정점을 매번 훑고 복사하지 않고, 브러시가 지나간 행·열과 실제 변경 정점만 계산하도록 최적화했다.

### 검증

- `npm test`, `npm run build`로 빠른 획의 연속성, 누적·지우기 결과, 고밀도 정점 상한과 프로덕션 빌드를 확인한다.

### 참고

- 5000칸급 격자는 정사각형 기준 약 2,600만 정점과 5,200만 삼각형이 필요해 현재 CPU 변형·Phaser 객체 렌더링 구조로는 실시간 편집이 불가능하다. GPU 스키닝 전용 렌더러가 생기기 전까지 안전한 상한을 사용한다.
- 테스트 전용 임시 코드, 실제 앱에 포함되지 않는 더미 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.36.1 — 2026-08-17

**Phase**: 일반 격자 초고밀도화와 외곽 클리핑 보정

### 변경

- 일반 격자 네 단계를 128 · 512 · 1024 · 5120칸으로 크게 벌려, 필요한 작업에서는 브러시 경계를 픽셀에 가깝게 세밀하게 다듬을 수 있다.
- 모든 격자의 알파 클리핑이 실루엣 바깥을 최소 2px 더 확인한다. 이미지 가장자리의 얇거나 반투명한 테두리도 영향 영역에 포함될 가능성을 높였다.

### 검증

- `npm test`, `npm run build`로 새 밀도 구간, 외곽 클리핑 여유와 프로덕션 빌드를 확인한다.

### 참고

- 일반 `높음`은 매우 큰 Mesh를 의도적으로 만든다. 큰 이미지에서는 메모리와 자동 가중치 계산 시간이 크게 늘 수 있으므로 필요한 경우에만 선택하는 것이 좋다.
- 테스트 전용 임시 코드, 실제 앱에 포함되지 않는 더미 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.36.0 — 2026-08-17

**Phase**: 격자 해상도 세분화

### 추가

- 격자 선택을 도트 그림용과 일반 그림용으로 나누고, 각 부류에 `최소` · `낮음` · `보통` · `높음` 네 단계를 제공한다.
- 일반 격자는 기존 최고 밀도인 108칸을 최소로 사용하며 144 · 192 · 240칸 단계를 추가해 브러시 경계를 더 부드럽게 칠할 수 있다.
- 두 격자 부류 이름을 지원하는 10개 언어 번역과 해상도 단계 회귀 테스트를 추가했다.

### 검증

- `npm test`, `npm run build`로 기존 격자 호환성, 새 단계의 순서와 정점 상한, 프로덕션 빌드를 확인한다.
- 브라우저에서 기존 흑백 선택 버튼 테마를 유지한 두 줄 격자 선택 UI를 확인한다.

### 참고

- Puppet JSON 구조는 그대로이며 기존 해상도 이름과 칸 수도 바꾸지 않았다. 일반 `높음`은 정점 수가 늘어 관절이 많은 프로젝트에서 자동 가중치 계산 시간이 길어질 수 있다.
- 테스트 전용 임시 코드, 실제 앱에 포함되지 않는 더미 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.35.0 — 2026-08-17

**Phase**: 일러스트 교체와 리그 재사용

### 추가

- `파일` 메뉴에 `일러스트 교체`를 추가했다. 새 스킨을 골라도 관절 계층, 영향 영역과 애니메이션을
  그대로 유지하며 새 파일명을 캐릭터 이름으로 사용해 다른 프로젝트로 저장할 수 있다.
- 크기와 비율이 다른 일러스트는 관절 위치와 칠한 영향 영역을 상대 좌표로 옮겨, 비슷한 실루엣이면
  관절을 조금만 다듬어 재사용할 수 있다.
- 교체 전 확인창과 메뉴 문구를 지원하는 10개 언어로 제공한다.

### 검증

- `npm test`, `npm run build`로 크기가 다른 Mesh의 상대 영향 영역 보존과 프로덕션 빌드를 확인한다.
- 브라우저에서 기존 리그를 유지한 일러스트 교체와 새 이름 저장 흐름을 확인한다.

### 참고

- Puppet JSON 형식은 그대로다. 실루엣 차이가 큰 그림은 교체 후 관절 위치와 영향 영역을 직접 다듬어야 한다.
- 테스트 전용 임시 코드, 실제 앱에 포함되지 않는 더미 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.34.1 — 2026-08-17

**Phase**: 관절 크기 키 축 독립성 수정

### 수정

- 세로 크기만 줄여도 가로 크기 키가 0에 가깝게 기록되던 문제를 수정했다. 크기 Track의 곱셈 방식과
  중립값 1에 맞는 전용 역산식을 사용한다.
- 우클릭 크기 드래그는 첫 4px의 주 이동 방향으로 축을 잠가, 손떨림 때문에 반대 축까지 바뀌지 않는다.
- 애니메이션에는 실제로 움직인 축의 키만 기록하며 손대지 않은 축에는 Track을 만들지 않는다.

### 검증

- `npm test`, `npm run build`로 Y축 0.5배 편집 시 X축 Track 미생성과 기존 0.8배 Track 조합을 확인한다.

### 참고

- Puppet JSON 형식은 그대로다. 잘못 찍힌 기존 크기 키는 자동 삭제하지 않으므로 필요하면 해당 키를
  삭제한 뒤 다시 편집해야 한다.
- 테스트 전용 임시 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.34.0 — 2026-08-17

**Phase**: 관절 크기 직접 편집과 레이어 우선도

### 추가

- 관절점을 우클릭한 채 가로·세로로 끌어 기준 자세의 크기를 찌그러뜨리거나 늘릴 수 있다.
- 애니메이션을 일시정지한 상태에서 같은 조작을 하면 해당 시점에 `scaleX` · `scaleY` 키를 찍는다.

### 변경

- 관절 목록을 앞→뒤 레이어 순서로 사용해, 영향 영역이 겹치면 앞쪽 관절 가중치가 우선한다.
- 빈 캔버스 우클릭과 휠클릭 화면 이동은 그대로 유지한다.

### 검증

- `npm test`, `npm run build`로 레이어 순서 반전, 키 타입과 프로덕션 빌드를 확인한다.
- 브라우저에서 관절 우클릭 크기 편집과 기존 빈 곳 화면 이동을 확인한다.

### 참고

- Puppet JSON 형식은 그대로다. 기존 `scaleX` · `scaleY` 필드와 Track을 사용한다.
- 테스트 전용 임시 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.33.1 — 2026-08-16

**Phase**: iPhone 파일 내보내기와 스프라이트 시트 저장 안정화

### 변경

- 프로젝트 저장과 게임 내보내기는 비동기 ZIP 준비가 끝난 뒤 사용자가 한 번 더 누르는 확인창을 거쳐
  iOS 공유 시트를 확실히 열며, 파일 앱·드라이브 등 원하는 저장 위치를 고를 수 있게 했다.
- 스프라이트 시트는 iPhone 공유 시트에 PNG 파일들을 직접 넘겨 `사진에 저장`을 사용할 수 있게 했다.
  PNG 다중 공유를 지원하지 않는 브라우저에서는 기존처럼 manifest를 포함한 ZIP으로 저장한다.
- 새 공유 확인창은 기존 어두운 흑백 테마와 iPhone 안전 영역을 따르고 지원 언어 10개로 번역했다.

### 검증

- `npm test`, `npm run build`로 ZIP 왕복, 스프라이트 시트 생성, 타입 검사와 프로덕션 빌드를 확인한다.

### 참고

- Puppet JSON 형식은 그대로다. 실제 iOS 공유 시트의 목적지 목록은 설치된 앱에 따라 달라진다.
- 테스트 전용 임시 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.33.0 — 2026-08-16

**Phase**: 영향 영역 전체 보정 강도와 안전장치

### 추가

- `모두 채우기`와 `정리`에 `약하게` · `보통` · `강하게` 단계를 추가했다. 강하게 채우기는 50% 이하로
  칠해진 정점까지 거리 기반 값으로 다시 채우며, 강하게 정리는 이웃 하나뿐인 오점과 8% 미만 잔여도 걷어낸다.
- 모든 관절에 영향을 주는 두 작업은 실행 전 현재 언어로 확인창을 표시한다.
- 실행 후 채운 빈 정점 수와 제거한 오점 수, `Ctrl+Z` 실행 취소 안내를 현재 언어로 표시한다.

### 변경

- 전체 보정 함수가 가중치와 함께 변경 통계를 반환해 UI가 실제 작업량을 정확히 안내한다.
- 버튼과 단계 선택은 기존 패널의 outline 버튼·chip 테마를 그대로 사용하며 지원하는 10개 언어를 모두 제공한다.

### 검증

- `npm test`, `npm run build`로 단계별 50% 가중치 처리, 변경 집계, 10개 언어 결과 문구와 전체 빌드를 확인한다.

### 참고

- Puppet JSON 형식은 그대로다. 테스트 전용 임시 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.32.0 — 2026-08-16

**Phase**: 영향 영역 전체 보정 도구

### 추가

- 선택 관절의 영향 영역 패널에 캐릭터 전체를 대상으로 하는 `모두 채우기`와 `정리` 버튼을 추가했다.
- `모두 채우기`는 기존에 칠한 값을 덮지 않고 이미지 실루엣 안의 빈 정점만 현재 관절 거리 가중치로 채운다.
- `정리`는 8방향 이웃이 없는 한 점짜리 오점과 3% 미만의 희미한 잔여 값을 제거한 뒤 빈 곳을 다시 채운다.

### 변경

- README 현재 상태에 전체 영역 보정 흐름을 추가하고 새 버튼과 설명을 지원하는 10개 언어로 번역했다.

### 검증

- `npm test`, `npm run build`로 기존 값 보존, 빈 정점 제거, 고립 오점 정리와 전체 빌드를 확인한다.

### 참고

- Puppet JSON 형식은 그대로다. 테스트 전용 임시 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.31.4 — 2026-08-16

**Phase**: 편집기 카탈로그 전체 언어 보완

### 수정

- 격자·전체 보기뿐 아니라 태그 그룹, 애니메이션 그룹과 모든 기본 프리셋 이름을 지원하는 10개 언어로 번역했다.
- 태그의 대상·움직임 보정·표시 전용 설명과 프리셋 설명을 모든 지원 언어에서 해당 언어의 문장으로 제공한다.
- 태그 배율, 태그 사용 애니메이션, 반복 여부·트랙 수, 프리셋 보유 여부처럼 값이 실행 중 바뀌는 툴팁을
  9개 비한국어 언어별 문장 틀로 조립해 영어 외 언어에서 한국어로 돌아가던 분기를 제거했다.

### 검증

- `npm test`, `npm run build`로 모든 지원 언어의 카탈로그 이름, 설명과 동적 툴팁에 한국어가 남지 않는지 확인한다.

### 참고

- Puppet JSON 형식과 UI 스타일은 바꾸지 않았다. 테스트 전용 임시 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.31.3 — 2026-08-16

**Phase**: 편집기 영어 UI 잔여 문구 보완

### 수정

- 격자 해상도의 최소·낮음·보통·높음과 캔버스의 전체 보기·연결 버튼을 지원 언어 번역에 추가했다.
- 애니메이션 선택기의 그룹, 이름, 설명과 태그 선택기의 그룹 및 41개 태그 설명을 영어 설정에서
  한국어 원문 대신 영어로 표시하도록 데이터 기반 UI도 명시적으로 번역 경로에 연결했다.
- 배율, 애니메이션 길이·트랙 수, 사용 동작 목록처럼 실행 중 값이 들어가는 태그·애니메이션 툴팁은
  영어 문장 틀에 값을 조립해 완성 문장 전체가 한국어 키와 일치해야만 번역되던 문제를 없앴다.

### 검증

- `npm test`, `npm run build`로 격자 단계, 전체 보기, 프리셋 설명, 태그 설명의 영어 회귀와 전체 빌드를 확인한다.

### 참고

- Puppet JSON 형식과 UI 테마는 바꾸지 않았다. 테스트 전용 임시 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.31.2 — 2026-08-16

**Phase**: 편집기 다국어 동적 UI 보완

### 수정

- 애니메이션, 영향 영역, 키, 변형 패널에서 실행 중 생성되는 대표 버튼과 제목을
  지원하는 10개 언어의 실제 번역으로 추가했다.
- 초기 HTML만 번역하던 처리를 DOM 변경 감지와 연결해, 상태 변경 후 다시 그려지는 글자,
  툴팁, `title`·`aria-label`·`placeholder` 접근성 문구도 현재 언어 설정을 자동으로 거치게 했다.

### 검증

- `npm test`, `npm run build`로 9개 비한국어 언어의 동적 조작부 번역과 전체 빌드를 확인한다.

### 참고

- Puppet JSON 형식은 바꾸지 않았다. 테스트 전용 임시 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.31.1 — 2026-08-16

**Phase**: 계층 변형 고정 보완

### 수정

- 몸통 → 윗다리 → 아랫다리 → 발처럼 부모가 여러 단계인 구조에서도 위치 고정과 완전 고정이
  자기 애니메이션 및 모든 부모 변환을 무시하도록 애니메이션별 변형 설정을 스킨 행렬에 반영했다.
- 완전 고정 관절이 정점의 주 영향일 때 남아 있는 다른 관절 가중치와 섞이지 않게 하여, 발 중심이
  부모 다리의 동작 때문에 미끄러지지 않도록 했다. 영향이 같은 경계는 기존처럼 부드럽게 섞인다.
- 편집기 미리보기, 스프라이트 시트 출력과 게임 런타임이 같은 고정 판정을 공유하도록 맞췄다.

### 검증

- `npm test`, `npm run build`로 4단계 다리 계층, 잔여 부모 가중치, 기존 경계 혼합과 전체 빌드를 확인한다.

### 참고

- Puppet JSON 형식은 바꾸지 않았다. 테스트 전용 임시 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.31.0 — 2026-08-16

**Phase**: 키프레임 직접 편집 UI 보완

### 추가

- 타임라인에 선택 관절의 움직이지 않은 자세를 현재 시각에 기록하는 `기본 키` 버튼과,
  모바일에서도 현재 직접 키를 지울 수 있는 `키 삭제` 버튼을 추가했다.
- 관절 속성에 첫 직접 키를 만들 때 0초와 애니메이션 끝에 원본 자세로 돌아오는 보호 키를
  자동으로 만든다. 보호 키도 일반 직접 키라 사용자가 옮기거나 지울 수 있다.
- 새 버튼과 툴팁 문구를 지원 언어 10개로 번역했다.

### 수정

- 버튼으로 키를 지운 직후 현재 시각의 자세, 움직인 관절점, 선택 링과 집기 판정을 다시 계산해
  남은 키의 결과 또는 원본 자세로 즉시 돌아오게 했다.
- 모바일 타임라인에서는 숫자 보조 정보 대신 기본 키·삭제 버튼과 실제 시간 축의 폭을 우선한다.

### 검증

- `npm test`, `npm run build`로 키 편집 회귀 테스트, 번역 완전성, 타입 검사와 프로덕션 빌드를 확인한다.
- 브라우저 데스크톱·모바일 뷰에서 버튼 배치와 키 생성·삭제 후 관절 오버레이 추적을 확인한다.

### 참고

- Puppet JSON 형식은 바꾸지 않았다. 보호 키는 기존 bone Track의 일반 키프레임으로 저장된다.
- 테스트 전용 임시 코드나 사용되지 않는 과거 코드는 추가하지 않았다.

## v0.30.2 — 2026-08-16

**Phase**: 편집기 다국어 설정 보완

### 수정

- 공용 툴팁이 화면에 나타나는 시점에 현재 언어 설정을 거치도록 바꿔, 타임라인처럼
  나중에 만들어지는 툴팁도 한국어에 고정되지 않게 했다.
- 타임라인의 처음으로, 재생/일시정지, 시간 축 툴팁 제목·본문·단축키 안내를
  지원하는 10개 언어의 실제 번역으로 추가했다.

### 검증

- `npm test`, `npm run build`로 다국어 번역 자료와 공용 툴팁 렌더링 경로, 전체 빌드를 확인한다.

### 참고

- 새 툴팁은 반드시 `i18n.ts`에 지원 언어 전체 번역을 같이 넣어야 한다. 파일 포맷과
  프로젝트 저장 내용은 바꾸지 않았다.

## v0.30.1 — 2026-08-16

**Phase**: 영향 영역 브러시와 변형 고정감 조정

### 변경

- 실제 변형에 쓰는 정규화 단계에서 칠한 농도의 차이를 제곱해 강조한다. 주요 영역을 50% 정도
  칠해도 10% 정도의 옅은 겹침보다 95% 이상 우세해져, 경계는 부드럽게 남기면서 중심 구역은
  선택한 관절에 거의 고정된다.
- 한 획 안에서도 같은 자리를 왕복하거나 선분이 겹치면 구간마다 농도가 더해진다. 손을 떼지
  않고 문질러도 수채화처럼 여러 겹이 쌓이며, 지우개도 같은 방식으로 여러 겹을 깎는다.

### 검증

- `npm test`, `npm run build`로 가중치 강조, 한 획 내 중첩, 기존 정규화와 빌드를 확인한다.

### 참고

- 편집용 원본 농도와 Puppet JSON 구조는 바꾸지 않았다. 강조는 저장용 정규화에만 적용되므로
  기존 파일도 다시 편집하거나 저장할 때 같은 고정감을 얻는다.

## v0.30.0 — 2026-08-16

**Phase**: 편집기 다국어 설정

### 추가

- 설정에 한국어, 영어, 중국어 간체·번체, 일본어, 스페인어, 프랑스어, 독일어,
  브라질 포르투갈어, 러시아어 선택을 추가하고 각 언어의 자칭으로 표시한다.
- 선택을 브라우저에 저장하며 첫 방문에는 브라우저 언어를 자동 감지한다.
- 정적 화면, 접근성 이름, 빈 프로젝트와 설정 핵심 문구에 실제 번역을 제공한다.

### 변경

- `CLAUDE.md`에 모든 새 UI 문구를 지원 언어 전체로 번역하는 규칙을 명시했다.

### 검증

- `npm test`, `npm run build` 및 브라우저에서 언어별 선택·유지·표시를 확인한다.

### 참고

- 파일 포맷은 바뀌지 않았고 언어 선택은 브라우저에만 저장된다.

## v0.29.1 — 2026-08-16

**Phase**: 휴대폰에서 화면이 저절로 확대되던 것

### 수정

- **더블탭으로 화면이 확대되던 것.** 뷰포트의 `user-scalable=no`로는 막히지 않는다 —
  **iOS Safari가 iOS 10부터 그 값을 무시한다.** 표준 방법인 `touch-action: manipulation`을
  쓴다. "평소 동작에서 더블탭 확대만 뺀 것"이라 스크롤과 두 손가락 조작은 그대로다.
  - `#app`에 걸고, 손가락으로 누르는 요소(버튼 · 입력칸 · 목록 줄)에도 직접 걸었다.
    규칙상 조상 값과 교집합이 되므로 하나로도 되어야 하지만, 정작 문제가 되는 것은
    iOS Safari 쪽이라 눌리는 요소에 직접 적어 두는 편이 확실하다
  - 요소 선택자로 적어서, 값을 끌어 정하는 곳(조절 막대 · 시간 축 · 색 팔레트 · 캔버스)의
    `touch-action: none`이 그대로 이긴다
  - **더블탭으로 이름 바꾸기는 그대로다.** 이 값은 브라우저의 확대 동작만 끄고
    `dblclick` 이벤트는 건드리지 않는다
- **입력칸을 누르면 화면이 튀어 오르던 것.** iOS는 글자가 16px보다 작은 입력칸에 손을 대면
  화면을 저절로 확대한다. 좁은 화면에서는 입력칸 글자를 16px로 둔다.
- 조절 막대와 가중치 그래프에 `touch-action: none`을 줬다. 손가락으로 끌 때
  브라우저가 먼저 화면을 끌고 가 버리던 것을 막는다.

### 참고

- **끝까지 확인하지 못한 것이 하나 있다.** 이 환경의 헤드리스 Chromium은 더블탭 확대 자체를
  재현하지 않는다. 일부러 되돌려 놓고 재 봐도 배율이 그대로였으므로, "확대되지 않았다"는
  측정은 아무것도 증명하지 못한다. 그래서 **적용 여부**(요소별 `touch-action` 계산값)까지만
  확인했다. 실제 확대가 멎었는지는 휴대폰에서 봐 주면 좋겠다.

### 검증

- 요소별 `touch-action` 계산값: 버튼 · 하단 탭 · 애니메이션 이름 · 입력칸 `manipulation`,
  캔버스 · 조절 막대 · 시간 축 `none`
- 더블탭으로 이름 바꾸기 여전히 동작
- 모바일 화면 조작 27가지 전부 통과, `npm test` 316개 통과

---

## v0.29.0 — 2026-08-16

**Phase**: 격자 한 단계 위 · 휴대폰에서 파일 받기

### 추가

- **더 촘촘한 격자 `ultra`(108칸).** 이름표를 한 칸씩 내려 지금까지의 `높음`(72칸)이
  `보통`이 되고, 그 위에 새 `높음`이 생겼다. 최소(24) · 낮음(48) · 보통(72) · 높음(108).
  - **값은 하나도 건드리지 않았다.** 파일에는 이름만 적히고 격자는 열 때 다시 만들어지므로,
    같은 이름의 칸 수를 바꾸면 예전 파일의 정점 수가 어긋나 **칠해 둔 영향 영역이 통째로
    날아간다.** 그래서 `low` · `normal` · `high`의 칸 수는 그대로 두고 이름표만 옮겼다.
    예전 파일을 열면 격자가 예전과 똑같다
  - 그림을 처음 불러올 때의 기본값도 `보통`(72칸)이 됐다. 도트 그림은 그대로 `최소`(24)다
  - `MESH_LABELS`가 이름표를, `MESH_GRID`가 칸 수를 따로 들고 있다.
    설정 화면은 이름표를 그대로 훑으므로 늘려도 화면 코드를 고칠 일이 없다

### 변경

- **휴대폰에서 내보내기 · 시트 굽기가 실제로 파일을 준다.** 전에는 `<a download>` 하나에
  기대고 있었는데, iOS Safari는 blob에 붙은 `download`를 무시하는 일이 잦다.
  그러면 눌러도 아무 일이 일어나지 않아 "안 된다"가 된다. 이제 순서를 둔다.
  1. **공유 시트**(`navigator.share`) — 파일 앱 · 드라이브 · 메신저 어디로든 보낼 수 있다.
     휴대폰에서는 이 방법이 가장 확실하다
  2. **내려받기** — 평소의 `<a download>`. 문서에 붙였다 떼는 것까지 한다.
     떠 있지 않은 링크의 클릭을 무시하는 브라우저가 있다
  3. **새 탭으로 열기** — 위 둘이 막힌 경우의 마지막 수단
  - 어느 방법으로 넘겼는지 상태줄에 적는다. 공유 시트가 뜨는 것이 정상인데 아무 말이 없으면
    또 "안 된다"로 읽히기 때문이다. 사용자가 공유 창을 닫으면 취소로 보고 파일을 남기지 않는다

### 검증

- `npm test` 316개 통과 (새 6개), `npm run build` · `tsc --noEmit` 통과
  - 예전 이름의 칸 수가 그대로인지, 이름표가 빠짐없이 있는지 테스트가 지킨다
- Chromium(Pixel 7)에서
  - 격자 선택지 `최소 · 낮음 · 보통* · 높음`, `높음`을 고르면 74×108 · 정점 8175로 바뀐다
  - 공유 시트를 가진 휴대폰인 척했을 때 내보내기와 시트 굽기가 모두 공유로 넘어간다
    (`human.export.zip 125KB` · `human.sheets.zip 386KB`)
  - 공유 창을 닫으면 "내보내기를 취소했습니다"로 끝나고 파일이 남지 않는다
  - 공유 시트가 없는 브라우저에서는 예전처럼 내려받기로 떨어진다

---

## v0.28.0 — 2026-08-16

**Phase**: 브러시를 그림 앱처럼

두 가지 지적이 사실은 같은 곳에서 나왔다. 칠하기가 "선을 긋는 일"이 아니라
"포인터가 보고될 때마다 그 자리에 원을 하나 얹는 일"이었다.

### 변경

- **한 획이 하나의 획이 된다** (`core/weight/stroke.ts`).
  - **빠르게 그어도 끊기지 않는다.** 직전 점과 이번 점을 잇는 **선분**으로 칠한다.
    브라우저가 포인터를 알려 주는 간격은 손 속도와 무관하게 일정해서, 점을 찍는 방식은
    빨리 그으면 원 사이가 벌어져 점선이 됐다.
  - **천천히 그어도 그 부분만 진해지지 않는다.** 획이 지나간 자리의 *가장 진했던 값*만
    모아 두었다가 시작할 때의 값 위에 **한 번** 얹는다. 같은 자리를 몇 번 훑든 한 겹이다.
    더 진하게 하려면 손을 떼고 다시 긋는다 — 그림 앱과 같은 규칙이다.
  - 지우개도 같은 방식으로 깎아 낸다.

### 수정

- **칠한 것이 Undo로 정확히 돌아오지 않던 것.** Undo에 프로젝트만 담고 있었는데,
  파일에 들어가는 `mesh.weights`는 정점마다 상위 네 개만 남기고 합을 1로 맞춘
  **정규화된 결과**다. 거기서 되짚어 낸 값은 칠할 때 쓰던 원본과 달라서, 되돌리면
  칠한 모양이 어긋난 채로 돌아왔다(가중치 합 256.5 → 216.5).
  편집용 원본 가중치를 함께 담아 그대로 복원한다.

### 검증

- `npm test` 310개 통과 (새 5개), `npm run build` · `tsc --noEmit` 통과
- Chromium에서 실제로 재 봤다
  - 브러시 지름보다 훨씬 멀리 **한 번에** 건너뛴 획: 13칸이 값 0.09로 고르게 이어지고
    중간에 빈 곳 0
  - 제자리에서 40번 문지르기와 한 번 콕 찍기가 **같은 값**(0.19 = 0.19)
  - 손을 떼고 다시 그으면 0.19 → 0.45로 쌓인다
  - 획 두 번 → Undo 한 번에 한 획씩 정확히 되돌아오고(211.14 → 211.14), 두 번이면 처음 값으로
  - 휴대폰에서 손가락으로 칠하기 · 지우기도 그대로 동작

---

## v0.27.2 — 2026-08-16

**Phase**: 좌우 반전이 꺼지지 않던 것

### 수정

- **좌우 반전이 켜지기만 하고 다시 눌러도 꺼지지 않던 것.**
  - 버튼의 눌림 처리가 **줄을 처음 그렸을 때의 애니메이션 객체**를 붙들고 있었다.
    조절 줄은 같은 애니메이션이 골라져 있는 동안 다시 만들어지지 않고 값만 갈아 끼우는데
    (막대를 끌던 중에 줄이 사라지면 드래그가 끊기기 때문이다), 그래서 그 객체의
    `mirror`는 영원히 처음 값(false)에 묶여 있었다. `!false`는 언제나 true다.
  - 누르는 시점에 스토어에서 다시 읽도록 고쳤다.
  - **모바일만의 문제가 아니었다.** 데스크톱에서도 똑같이 꺼지지 않았다.

### 참고

- 같은 종류(다시 만들어지지 않는 자리에서 바깥 객체를 붙들고 토글)를 UI 전체에서 훑었다.
  나머지 토글은 인스턴스 값이나 스토어를 그때그때 읽고 있어 문제가 없다.

### 검증

- 데스크톱(1280×860)과 Pixel 7에서 각각
  - 켜기 → 끄기 → 켜기가 세 번 다 제대로 뒤바뀐다
  - `되돌리기`로도 꺼진다
  - `attack`만 켜면 `walk`는 그대로고, `walk`로 돌아오면 버튼도 꺼진 채로 나온다
- `npm test` 305개 통과, `npm run build` · `tsc --noEmit` 통과

---

## v0.27.1 — 2026-08-16

**Phase**: 모바일 전체 점검

"모바일에서 편집이나 설정 탭이 안 눌린다"는 지적을 확인하러 갔다가 두 가지 실제 결함을 찾았다.
둘 다 화면에는 버튼이 멀쩡히 보이는데 손가락이 닿지 않는 종류라, 고장 난 것처럼 보였다.

### 수정

- **파일 · 설정 메뉴가 열렸다가 바로 닫히던 것.** 상단 바에 준 `overflow-x: auto`가
  그 안에 떠 있는 메뉴 패널을 **바 높이(40px)에 잘라 버렸다.** 화면에는 보이지만 그 자리를
  누르면 뒤에 있는 캔버스가 눌리고, 캔버스를 누르면 메뉴가 닫히므로 "안 눌린다"가 됐다.
  (`elementFromPoint`로 확인하니 도트 버튼 자리에서 `CANVAS`가 잡혔다.)
  - 애니메이션 바에서 v0.25.0에 고쳤던 것과 **똑같은 실수**를 상단 바에서 되풀이했다.
    넘치는 것은 안쪽 스크롤 영역(`.layers`)이 흡수하게 하고 바에는 overflow를 주지 않는다
- **거의 모든 버튼이 두 번 눌러야 먹히던 것.** v0.27.0의 "터치는 두 걸음" 규칙을
  툴팁이 붙은 곳 **전부**(25군데)에 적용해 버렸다. 변형 · 프리셋 · 좌우 반전 · 설정까지
  전부 두 번씩 눌러야 했다.
  - 두 걸음은 이제 `explainOnTouch`로 **켜는 곳만** 쓴다. 지금은 태그 하나다 —
    마흔 개가 넘고 이름만으로는 무엇이 움직일지 알 수 없어 읽고 나서 골라야 하는 자리다
  - 나머지는 손가락으로 눌러도 곧바로 실행되고, 설명이 그 위에 잠깐 떴다 사라진다
- **하단 시트가 애니메이션 바를 덮던 것.** 시트를 연 채로는 재생도 프리셋 추가도
  손이 닿지 않는데 버튼은 그대로 보였다. 시트를 애니메이션 바(와 타임라인) **위**에 세운다.
- **화면이 가로로 63~100px 넘치던 것.** 격자 칸은 기본이 `min-width: auto`라 내용보다
  좁아지지 않아, 상단 · 하단 바가 화면 밖으로 자라고 캔버스까지 그 폭을 따라갔다.
  `#app > * { min-width: 0 }`으로 줄어들 수 있게 열어 두면 각자의 스크롤 영역이 흡수한다.

### 검증

Chromium을 Pixel 7(412×839) · iPhone 13 mini(375×629)로 띄우고, 개발본과 **빌드 결과물**
양쪽에서 손가락으로만 조작했다. 캔버스 조작은 CDP의 실제 터치 이벤트로 보냈다 —
직접 만들어 보낸 이벤트는 Phaser의 입력을 지나가지 않아 통과해도 의미가 없다.

- 화면 조작 **27가지 전부 통과**, 콘솔 오류 0, 가로 넘침 0
  - 파일 · 설정 메뉴, 설정 안의 값 고르기, 표시 토글
  - 관절 추가 · 변형 고르기 · 태그(1번 설명 → 2번 부착) · 칠하기 켜고 끄기
  - 프리셋 담기 · 이름 눌러 재생 · 좌우 반전 · 타임라인 · 숨김 · 삭제 · Undo/Redo · 내보내기
- 실제 터치로 캔버스: 관절 끌어 옮기기 · 빈 곳 끌어 화면 이동 · 두 손가락 확대(1.7 → 4.9)
- 손가락으로 칠하기 · 지우기: 가중치 합 191 → 253 → 151 (정점 개수는 이미 다 칠해져 있어
  변하지 않는다. 값으로 재야 보인다)

---

## v0.27.0 — 2026-08-16

**Phase**: 좌우 다루기와 손가락으로 읽는 설명

좌우 반전이 캐릭터 하나에 하나뿐이라 쓸 자리가 좁았다. 그리고 휴대폰에서는
태그 설명을 읽을 방법이 아예 없었다 — 손가락에는 "올리기"가 없기 때문이다.

### 추가

- **좌우 반전이 애니메이션마다 따로다** (`PuppetAnimation.mirror`).
  - 하단 조절 줄의 `좌우 반전` 버튼. 켜면 그 동작만 반대쪽으로 나간다
  - 오른쪽을 보고 걷다가 왼쪽으로 후려치는 캐릭터를 만들 수 있고,
    같은 프리셋을 두 벌 담아 좌우 한 쌍으로 쓸 수도 있다
  - `되돌리기`가 이 값도 함께 끈다
- **설정에 `좌우 뒤집기`.** 이쪽은 **데이터를 실제로 옮긴다** — 그림의 픽셀, 관절 자리,
  칠해 둔 영향 영역까지 전부.
  - 왼쪽을 보는 그림을 받았을 때 아예 오른쪽을 보는 캐릭터로 만드는 용도다
  - 포맷에 아무 표시도 남지 않는다. 내보낸 결과를 받는 게임 쪽에서는
    처음부터 그런 그림이었던 것과 구분되지 않는다
  - Undo 한 단위이고, 한 번 더 누르면 돌아온다
- **손가락으로도 설명을 읽는다.** 터치에서는 누름이 두 걸음으로 나뉜다.
  - 첫 번째 누름: 설명이 뜨고 **실행되지 않는다**
  - 두 번째 누름: 설명이 닫히고 그제서야 실행된다
  - 다른 곳을 누르면 설명만 닫히고 처음 상태로 돌아간다 —
    이걸 두지 않으면 나중에 그 버튼을 누를 때 "두 번째"로 잘못 세어져 바로 실행된다
  - 마우스 동작은 그대로다(올리면 뜨고 비키면 사라진다)

### 변경

- **`character.facing`을 없앴다.** 캐릭터 단위 값이었던 것을 애니메이션 단위로 옮겼다.
  v13 파일을 열면 그 값이 모든 동작의 `mirror`로 옮겨 담긴다 — 보이던 모습이 유지된다.
- 좁은 화면에서 툴팁을 감추던 규칙을 걷어냈다. 이제 폭만 화면에 맞춘다.

### 참고

- Puppet JSON **v14**. `character.facing` → `animation.mirror`.
- `설정의 좌우 뒤집기`와 `애니메이션의 좌우 반전`은 다른 일이다.
  전자는 그림과 관절을 옮기고, 후자는 재생할 때 방향만 돌린다.

### 검증

- `npm test` 305개 통과 (새 11개), `npm run build` · `tsc --noEmit` 통과
- 뒤집기는 두 번 하면 처음으로 돌아온다는 것을 관절 · 칠한 영역 · 프로젝트 단위로 못 박았다

---

## v0.26.1 — 2026-08-16

**Phase**: 애니메이션 목록 정리

"재생이 좌측 하단에도 있고 눌러서 뜨는 탭에도 있어 중복인 것 같다"는 지적에서 출발했다.
둘은 다른 것이었지만 — 아래는 **가진 것**, 위는 **고르는 곳** — 화면이 그렇게 말해 주지 않았다.
확인하다가 더 큰 문제를 찾았다.

### 수정

- **새 프리셋 7개가 목록에 아예 나오지 않던 것.** 프리셋 고르는 목록이 갈래를
  `["기본", "공격"]`로 직접 적고 있어서, v0.26.0에서 `이동` · `반응` 갈래를 늘렸을 때
  그 줄을 함께 고치지 않았다. 달리기 · 비행 · 헤엄 · 방어 · 회피 · 기절 · 승리를
  만들어 두고도 쓸 수가 없었다.
  - 갈래 순서를 `PRESET_GROUPS`로 데이터 쪽에 두고 화면은 그것을 그대로 훑는다
  - `tests/presets.test.ts`가 모든 프리셋의 갈래가 그 목록에 있는지 확인한다.
    이제 갈래를 늘려도 화면 코드를 따로 고칠 일이 없고, 빠뜨리면 테스트가 깨진다

### 변경

- **고르는 목록이 "추가하는 곳"으로 보이게 했다.** 아래 줄과 헷갈리지 않게 하기 위한 것이다.
  - 머리말 `애니메이션 추가 · 고르면 아래 목록에 담기고 바로 재생됩니다`
  - 버튼마다 ＋를 붙여 누르면 담긴다는 것을 그림으로도 말한다
  - **이미 담은 것은 `담김`으로 흐리게 표시한다.** 막지는 않는다 — 사본을 일부러
    만들 수도 있기 때문이다(`walk2`)

### 검증

- `npm test` 294개 통과 (새 2개), `npm run build` · `tsc --noEmit` 통과
- Chromium(데스크톱 · Pixel 7)에서 확인
  - 갈래 넷(`기본 · 이동 · 반응 · 공격`)이 모두 나오고 프리셋 버튼 **23개 전부**에 ＋가 붙는다
  - `돌진`을 담으면 하단 목록이 `idle → idle, charge`가 되고,
    다시 열었을 때 `대기` · `돌진`이 `담김`으로 표시된다

---

## v0.26.0 — 2026-08-16

**Phase**: 동작의 질과 손맛 (좌우 반전 · 프리셋 확장 · 표시 · 아이콘)

한 번에 여덟 가지를 손봤다. 공통점은 전부 "쓰다 보니 걸리는 것"이라는 점이다.

### 추가

- **좌우 반전** (`character.facing`). 왼쪽을 보는 그림을 넣으면 동작이 등 뒤로 나가던 것.
  - 프리셋은 전부 오른쪽을 보는 캐릭터 기준이다. `설정 → 보는 쪽`을 `왼쪽`으로 두면
    가로 이동과 회전의 부호가 뒤집혀 앞으로 내지른다
  - **그림이 아니라 동작을 뒤집는다.** 관절이 놓인 자리는 그대로다
  - 파일에 저장되어 내보내기 · 런타임 · 스프라이트 시트까지 그대로 따라간다.
    게임 쪽에서 챙길 것은 없다
  - 재생 중에 바꿔도 멈추지 않고 곧바로 방향이 바뀐다
- **새 프리셋 10개.** 공격과 할퀴기가 비슷하다는 지적에서 시작해, 성격이 확실히 다른 것들로 늘렸다.
  - 이동: `달리기`(앞으로 기운 자세 · 큰 보폭) · `비행`(날개가 주인공) · `헤엄`(몸을 훑는 물결)
  - 반응: `방어`(낮추고 버틴다) · `회피`(0.45초) · `기절`(제어를 잃고 휘청) · `승리`(뛰어오르며 팔 들기)
  - 공격: `돌진`(몸 전체가 멀리 나가고 되돌아오지 않는다) · `회전 베기`(제자리에서 한 바퀴) ·
    `내려찍기`(세로가 주인공)
  - 프리셋 13개 → **23개**, 갈래도 `기본` `이동` `반응` `공격` 넷으로 나눴다
- **아이콘을 전부 직접 그렸다** (`editor/ui/icons.ts`). 16×16 SVG 12종.
  - 글꼴 기호(▶ ■ ↩ × ⠿ …)는 기기마다 모양과 크기가 다르고 컬러 이모지로 바뀌기도 한다
  - 색은 언제나 `currentColor`라 버튼이 흐려지거나 강조되면 아이콘도 함께 따라간다
  - 면으로 볼 것(재생 · 정지)은 채우고 획으로 볼 것(＋ × 화살표)은 선으로 그린다

### 변경

- **사망 모션을 다시 만들었다.** 전에는 제자리에서 찌그러지기만 하고 넘어지지 않았다.
  - 무릎이 먼저 꺾이고 → 몸 전체가 81도 기울어 넘어가고 → 바닥에 닿은 뒤 머리가 늦게 떨어진다
  - 찌그러짐은 **바닥에 닿는 순간에만** 온다. 계속 눌려 있으면 죽는 게 아니라 녹는 것처럼 보인다
  - 눈을 감고, 늘어지는 부속은 끝까지 흘러내린다. 1.4초 → 1.6초, 트랙 5개 → 22개
- **할퀴기를 공격과 갈랐다.** 공격은 몸이 앞으로 나가며 한 번 크게 내지르고,
  할퀴기는 제자리에서 짧게 두 번 긁는다(타격 이벤트도 두 번).
- **흔들림과 강도가 서로 다른 일을 한다.** 전에는 둘 다 모든 값에 똑같이 곱해져 사실상 같았다.
  - `강도`는 **움직임의 세기** — 위치 · 회전 · 크기 전부. 0이면 아무것도 움직이지 않는다
  - `흔들림`은 **회전 계열에만** 한 번 더. 나아가는 거리는 그대로 두고 도는 느낌만 조절한다
- **완전 고정이 가중치에 비례해 동작한다.** 전에는 1%만 걸쳐도 그 정점이 통째로 얼어붙었다.
  100을 받은 자리는 아예 멈추고 50을 받은 자리는 절반만 움직인다. 경계가 더 이상 딱 끊기지 않는다.
- **형태 유지(rigid)는 그 정점의 주인일 때만** 통째로 움직인다. 검이 스치듯 걸친 자리까지
  검을 따라가면, 정작 그 자리를 대부분 맡은 팔의 움직임이 사라진다.
- **영향 영역을 점 패턴에서 면으로 바꿨다.**
  - 픽셀마다 값을 읽어 채우고 **가중치를 그대로 불투명도**로 쓴다(0.30~0.92).
    옅으면 조금만 따라오고 진하면 통째로 따라온다는 뜻이라, 보이는 대로 움직인다
  - 칠하지 않은 곳을 덮는 어둠도 0.40 → 0.62로 올려 경계가 또렷해졌다
  - 표시용 캔버스는 긴 변 640px로 줄여 굽는다. 점을 찍던 때보다 오히려 가볍고 매끄럽다
- **캔버스를 만지면 위에 떠 있던 목록과 메뉴가 내려간다.** 좁은 화면에서 프리셋 목록이
  화면 절반을 덮어, 재생을 구경하려고 눌렀는데 정작 캐릭터가 가려져 있었다.
- 조절 막대에서 손을 떼는 시점을 받아 드래그 한 번을 Undo 한 단위로 묶는다.

### 참고

- Puppet JSON **v13** (`character.facing`). 없으면 `"right"`로 본다. 예전 파일은 그대로 열린다.
- `evaluateAnimation`에 다섯 번째 인자 `mirror`가 생겼다. 기본값 false라 기존 호출은 그대로다.
- 추천 태그에서 `stiff`를 뺀 곳: 독침 · 무기 · 방패. 배율 태그가 된 뒤로는 붙이면
  정작 공격이 0.2배로 죽는다. 형태를 지키는 것은 변형(rigid)의 몫이다.

### 수정

- **흔들림 막대를 끌어도 아무 변화가 없던 것.** 재생 커서가 애니메이션 **객체를 붙들고** 있어서,
  스토어의 값만 바뀌고 화면에는 옛 값이 계속 쓰였다. 속도 · 강도는 커서가 따로 들고 있어
  괜찮았지만 흔들림은 데이터 자체에 적히는 값이라 그대로 흘렀다. 이것이 "흔들림과 강도의
  차이가 없다"의 진짜 원인이었다. 길이와 흔들림이 바뀌면 커서를 갈아 끼운다.

### 검증

- `npm test` 292개 통과 (새 21개), `npm run build` · `tsc --noEmit` 통과
- 새로 못 박은 것
  - 사망: 몸이 60도 넘게 기울고, 바닥으로 25px 넘게 내려가고, 찌그러짐은 후반에만 온다
  - 공격 대 할퀴기: 몸통 이동량이 3배 이상 차이 난다
  - 돌진은 다른 모든 공격보다 멀리 나가고, 회전 베기는 한 바퀴(>1.8π)를 돈다
  - 흔들림 2배 → 회전만 2배, 이동은 그대로. 강도 0 → 흔들림이 켜져 있어도 전부 멈춤
  - 완전 고정 100/50/0 → 0 / 절반 / 전부 이동
- Chromium(Playwright)에서 확인
  - SVG 아이콘 19개가 14×14로 그려지고, 버튼에 남은 글꼴 기호 **0개**
  - 타임라인 버튼이 `play` ↔ `pause`로 실제로 바뀐다
  - 좌우 반전: 팔이 가장 멀리 가는 x가 `+18 → −18`로 뒤집히고 파일에 `facing: "left"`로 남는다
  - 흔들림 1 → 1.95: 회전 `0.0348 → 0.0679`(×1.95), 이동 `3 → 3`(그대로)
  - 강도 1 → 1.95: 회전과 이동이 **함께** ×1.95. 강도 0이면 둘 다 0
  - 영향 영역: 가로 한 줄의 경계 전환이 2번(점 패턴이면 수십 번), 투명도 39단계(80~230)
  - 모바일에서 화면을 탭하면 프리셋 목록과 속성 시트가 함께 내려간다

---

## v0.25.1 — 2026-08-16

**Phase**: 배포본 점검

배포되는 것은 개발 서버가 아니라 빌드 결과물이다. 그동안의 검증이 전부 개발 서버에서만
이뤄져 있었기에, `vite preview`로 실제 빌드본을 띄워 다시 확인했다.

### 수정

- **탭 아이콘이 없어 방문할 때마다 `/favicon.ico`가 404**가 나던 것.
  관절 두 개와 연결선을 그린 SVG를 `index.html`에 data URI로 적었다 —
  이 툴이 캔버스에 그리는 것 그대로다. 파일을 늘리지 않고 요청도 늘지 않는다.

### 참고

- `window.__puppet` 디버그 훅은 `import.meta.env.DEV` 안에 있어 배포본에는 없다.
  의도된 것이며, 배포본 검증은 화면과 상태줄만 보고 해야 한다.
- Puppet JSON 포맷은 v12 그대로.

### 검증

- 빌드본(`vite preview`)을 데스크톱(1280×820)과 Pixel 7(412×839)에서 확인
  - 첫 캔버스까지 743ms · 669ms
  - 관절을 놓으면 "영향 영역을 자동으로 칠했습니다", 재생하면 **움직인다**
  - 시트 굽기 동작 (2장 · 48프레임)
  - 두 화면 모두 가로 넘침 0, 실패 요청 · 콘솔 오류 없음

---

## v0.25.0 — 2026-08-16

**Phase**: 모바일 대응 (세로형 배치 · 터치 조작 · 갤러리)

**휴대폰에서는 아예 쓸 수 없었다.** 좌우 패널이 캔버스를 양쪽에서 눌러 그림이 손톱만 해졌고,
화면을 옮기려면 우클릭 드래그가 필요한데 손가락에는 오른쪽 버튼이 없었으며,
갤러리를 열어도 사진이 하나도 보이지 않았다.

### 추가

- **세로형 배치.** 좁은 화면에서는 좌우 패널이 화면 밖으로 내려가고, 하단 `관절` / `속성`
  탭으로 필요할 때만 끌어 올린다. 캔버스가 화면의 **85%**를 차지한다. (기획서 33)
  - 같은 탭을 다시 누르거나 캔버스를 만지면 내려간다
  - 관절을 추가하거나 칠하기를 켜면 저절로 내려간다 — 둘 다 곧바로 캔버스를 봐야 하는 일이다
  - 폭뿐 아니라 **높이**도 본다. 휴대폰을 눕히면 폭은 넉넉해지지만 세로가 400px대라,
    그대로 두면 캔버스가 화면의 38%로 줄어든다 (기준을 넓힌 뒤 68%)
- **터치 조작** (`EditorScene.attachTouchGestures`).
  - 두 손가락을 벌리면 확대, 오므리면 축소. 두 손가락 사이의 그림이 그 자리에 머문다
  - 두 손가락을 함께 끌면 화면이 따라 움직인다
  - 한 손가락으로 빈 곳을 끌어도 화면이 움직인다 — 마우스의 우클릭 드래그를 대신한다
  - 손가락이 하나 더 얹히면 그때까지 하던 조작(관절 끌기 · 칠하기)을 없던 일로 되돌린다.
    화면을 넓히려던 것이지 관절을 옮기려던 것이 아니기 때문이다
  - 마우스 조작은 이 코드를 지나가지 않으므로 그대로다
- **안내판을 눌러서 사진 고르기.** 손가락으로는 끌어다 놓을 수 없으므로,
  좁은 화면에서는 문구가 "눌러서 갤러리에서 사진 고르기"로 바뀌고 누르면 바로 열린다.

### 변경

- **갤러리가 사진을 걸러 내지 않는다.** 파일 입력을 `image/png,image/webp` →
  `image/*`로 바꿨다. 그 전에는 휴대폰 갤러리가 사진을 거의 다 회색으로 막아 놓았다.
- **JPEG를 막지 않는다.** 갤러리는 거의 전부 JPEG인데, 고른 사진이 "쓸 수 없습니다" 한 줄로
  튕겨 나오면 그 사람은 그대로 툴을 닫는다. 투명 배경이 없으면 배경까지 함께 휘는 것뿐이므로,
  막는 대신 열어 주고 상태줄로 알려 준다.
- 브라우저의 페이지 확대를 막았다(`user-scalable=no`). 페이지째 확대되면 관절을 집을 수 없다.
- 손가락으로 집는 것들의 최소 크기를 키웠다(태그 · 도구 34px, 조절 막대 · 시간 축 22px).
- 툴팁은 좁은 화면에서 숨긴다. 마우스를 올릴 수 없는 곳에서는 방해만 된다.

### 수정

- **좁은 화면에서 프리셋 목록과 조절 막대가 잘려 손이 닿지 않던 것.** 애니메이션 바에
  `overflow-x`를 준 것이 원인이었다. 두 패널이 그 안에 `absolute`로 떠 있어서 바 높이에
  잘려 나갔다. 목록만 밀도록 되돌렸다. (Chromium에서 클릭이 캔버스에 가로막혀 잡아냈다)
- **표시 토글의 글자가 "관/절"로 반토막 나던 것.** 한 줄로 붙이고 넘치면 옆으로 민다.
- **타임라인의 시간 축이 손가락 두 개 폭으로 줄어들던 것.** 숫자와 안내가 자리를 다 먹었다.
  좁은 화면에서는 안내를 숨기고 축에 폭을 넘긴다.

### 참고

- Puppet JSON 포맷은 그대로 v12다. 저장 파일에 영향이 없다.
- 아직 남은 것: Phaser 제거(페이지 무게의 91%), npm 재배포.

### 검증

- `npm test` 271개 통과, `npm run build` · `tsc --noEmit` 통과
- Chromium(Playwright)에서 기기 세 가지로 확인
  - Pixel 7 (412×839) · iPhone 13 mini (375×629) · 가로 (839×412)
  - 세 기기 모두 가로 넘침 0, 하단 탭 동작, 안내판을 누르면 파일 고르기가 열린다
  - 두 손가락 벌리기: 확대율 1.691 → 5.073
  - 두 손가락 끌기: 화면이 움직이고 확대율은 5.073 그대로
  - JPEG를 넣으면 Mesh가 생기고 상태줄이 투명 배경 안내를 말한다
- 데스크톱 동작은 그대로임을 v0.24.0의 검증 스크립트로 다시 확인

---

## v0.24.0 — 2026-08-16

**Phase**: 경량성 · 사용 편의성 점검에 따른 손질 (자동 가중치 · 길이 조절 · 태그 정리 · 시트 내보내기)

**칠하지 않으면 아무것도 움직이지 않았다.** 이미지를 넣고 관절을 다섯 개 놓고 재생을
눌러도 그림은 그대로였고, 상태줄은 태연히 "재생: walk"라고 말했다. 처음 쓰는 사람은
그 지점에서 툴이 고장 났다고 생각한다. 이번 항목의 절반은 그 절벽을 없애는 일이다.

### 추가

- **자동 가중치** (`core/weight/auto.ts`). 관절을 놓으면 영향 영역이 바로 깔린다.
  - 관절 하나를 **부모에서 자기까지의 선분**으로 보고, 정점마다 거리를 재서 가까운 쪽에
    많이 준다. 무릎 · 팔꿈치처럼 이어져 있으면 선분들이 팔다리를 알아서 나눠 가진다
  - 정점마다 합이 1이 되게 미리 나눠 둔다 — 화면의 영향 영역 표시와 실제 적용될 값이 같다
  - 관절을 더하거나 옮기거나 지울 때, 격자 해상도를 바꿀 때마다 다시 계산한다
  - **한 번이라도 직접 칠하면 그 관절은 사용자의 것이 된다.** 이후 자동 계산이 건드리지
    않는다. 우측 `영향 영역` 칸의 `자동` / `직접` 칩으로 언제든 되돌릴 수 있다
  - 직접 칠한 관절도 계산에는 들어간다. 옆에 있는 자동 관절이 1을 독차지하지 않게 하기 위한 것
- **애니메이션 길이 조절.** 하단 조절 막대 맨 앞에 `길이`가 생겼다 (0.1~10초).
  - 키와 이벤트 시각이 같은 비율로 함께 움직여 동작의 모양은 그대로다
  - 재생 `속도`와 다르다 — 속도는 재생할 때 곱하는 값이고, 길이는 파일에 그대로 적혀
    게임이 읽는 길이가 된다
- **스프라이트 시트로 굽기** (`파일` 메뉴). 숨기지 않은 애니메이션이 **하나도 빠짐없이
  각각 한 장씩** 나온다. 빼고 싶으면 목록에서 숨기면 된다.
  - 게임에서 도는 것과 완전히 같은 경로로 굽는다 (`evaluateAnimation → computeSkinMatrices
    → skinVertices`). 화면에서 본 것과 시트가 달라지지 않는다
  - 따라 흔들림도 담긴다. 반복 애니메이션은 한 바퀴를 미리 돌려 흔들림이 자리를 잡은 뒤의
    두 번째 바퀴를 쓴다 — 마지막 프레임과 첫 프레임이 이어지게 하기 위한 것
  - 모든 프레임을 담는 사각형을 재서 칸 크기를 정한다. 팔을 크게 휘둘러도 잘리지 않는다
  - `sheets.zip` 안에 애니메이션별 png와 `sheets.json`(칸 크기 · 열 · 행 · fps · 원점)
  - Phaser를 쓰지 않는다. WebGL2로 직접 그린다
- **성격 태그가 실제로 동작한다.** `heavy` 0.55배 · `light` 1.6배 · `bounce` 1.35배 ·
  `stiff` 0.2배. 이 관절이 받는 모든 움직임에 곱해진다 (`TAG_AMPLITUDE`).

### 변경

- **태그 41개 중 29개가 아무 일도 하지 않던 것을 없앴다.** 이제 전부 무언가를 한다.
  - `TagInfo.effect`로 갈래를 못 박았다 — `track`(프리셋이 찾는 대상) · `modifier`(움직임
    배율) · `hint`(표시용, 움직임 없음). `ground` 하나만 `hint`다
  - 프리셋 7개에 트랙 41개를 더해 `body` `spine` `hip` `neck` `foot` `finger` `tail`
    `wing` `tentacle` `hair` `ear` `horn` `fin` `cloth` `antenna` `eye` `shield` `prop`
    `decoration` `float` `front` `back` `upper` `lower`를 전부 실제 대상으로 만들었다
  - 대기에서 눈을 깜빡이고, 이동에서 앞뒤 다리가 엇갈리고, 공격에서 위쪽 팔이 크게
    휘두르고, 사망에서 촉수가 늘어진다
  - 태그 툴팁이 갈래에 맞는 말을 한다. 성격 태그에는 배율을, 표시용에는 "움직임을 만들지
    않습니다"를 적는다
  - `tests/tags.test.ts`가 이것을 지킨다. `track`으로 적힌 태그가 어느 프리셋에도 없으면
    테스트가 깨진다
- **독침 · 무기 · 방패의 추천 태그에서 `stiff`를 뺐다.** 배율 태그가 된 이상, 찌르는
  부위에 붙이면 정작 공격 동작이 0.2배로 죽는다. 형태를 지키는 것은 변형(rigid)의 몫이다.
  (전갈 테스트가 이것을 잡아냈다)
- **조절 막대 드래그가 Undo를 한 단위로 쌓는다.** 전에는 막대를 한 번 끄는 동안
  히스토리가 수십 개 쌓여 되돌리기가 사실상 동작하지 않았다.

### 참고

- Puppet JSON **v12**. Bone에 `autoWeight`가 생겼다. 없으면 꺼진 것으로 본다 —
  예전 파일의 관절은 전부 손으로 칠한 것이라 자동 계산이 덮어쓰면 작업이 사라진다.
- 아직 남은 것: Phaser 제거(페이지 무게의 91%), npm 재배포.

### 검증

- `npm test` 271개 통과 (새 24개), `npm run build` · `tsc --noEmit` 통과
- Chromium(Playwright)에서 실제로 재고 확인
  - **절벽이 사라졌다.** 이미지 → 관절 5개 → `이동` → 재생: 칠해진 정점 0개 → **398개**,
    "전혀 안 움직인다" → **"움직인다"**
  - 칠하면 칩이 `자동` → `직접`으로 바뀌고, 관절을 더 놓아도 직접 칠한 값이 그대로 남는다
  - 길이 막대를 끌면 `0.9초 → 3.55초`, 키가 `0, 0.25, 0.42, 0.9` → `0, 0.9861, 1.6567, 3.55`로
    같은 비율로 옮겨지고 이벤트(0.42초)도 함께 옮겨진다. Undo 한 번으로 전부 되돌아온다
  - 시트 굽기: `human.sheets.zip` 455KB · idle 32프레임 6×6 · attack 18프레임 5×4.
    칸마다 불투명 픽셀이 다르고(움직인다), 반복 애니메이션의 첫/마지막 프레임이 이어진다

---

## v0.23.0 — 2026-08-16

**Phase**: 키프레임 편집 3단계 마무리 (트랙 목록 · 보간 방식)

키를 찍고 옮기고 지울 수는 있었지만, **무엇이 찍혀 있는지 한눈에 볼 수 없었고
키에서 다음 키로 어떻게 이어질지 고칠 수도 없었다.**

### 추가

- **우측 패널에 `키` 칸.** 고른 관절이 그 애니메이션에 직접 찍은 Track을 속성 이름과
  개수로 보여 준다 (`가로 이동 1개` · `회전 3개`). 프리셋(태그)의 키는 나오지 않는다 —
  손댈 수 없는 것을 보여 주면 헷갈린다.
  - 키가 없으면 "일시정지한 뒤 관절을 끌면 그 시점에 키가 생깁니다"라고 알려 준다
- **보간 방식 고치기.** 재생 헤드가 키 위에 서 있을 때만 나온다.
  - `부드럽게` — 시작과 끝에서 느려진다. 살아 있는 것의 움직임
  - `일정하게` — 처음부터 끝까지 같은 속도. 기계처럼 딱딱한 움직임
  - `뚝 끊어서` — 중간을 만들지 않는다. 도트 애니메이션의 프레임 전환
  - 각 버튼에 어떤 경우에 쓰는지 예까지 툴팁으로 붙였다
  - 한 시각의 키를 모든 속성에서 함께 바꾼다
- **패널에서도 키를 지운다.** `0.30초 키 지우기` 버튼. 타임라인 우클릭 · `Delete`와 같다.
- **`ownKeysAt` · `ownTracks` · `setOwnKeyEase`** (`core/animation/edit.ts`).
  모두 `{kind:"bone"}` Track만 본다.

### 수정

- **바꾼 값이 화면에 바로 반영되지 않던 것.** 애니메이션을 갈아 끼울 때 스토어를 먼저
  갱신하면 그 자리에서 화면을 다시 그리는데, 그때 재생 커서가 아직 옛 애니메이션을
  들고 있어 방금 바꾼 값이 보이지 않았다. 커서를 먼저 갈아 끼우도록 순서를 바꿨다.
  (보간 버튼을 눌러도 강조가 옮겨 가지 않던 증상)
- 애니메이션 이름이 제목에서 대문자로 바뀌던 것. 사용자가 지은 이름이라 그대로 둔다.

### 검증

- `npm test` 247개 통과 (새 9개), `npm run build` · `tsc --noEmit` 통과
- Chromium(Playwright)에서 확인
  - 키를 찍으면 트랙 목록에 `가로 이동 1개` · `세로 이동 1개`가 뜬다
  - `뚝 끊어서`를 누르면 버튼 강조가 옮겨 가고 저장된 값도 `x:step · y:step`이 된다
  - 재생 헤드를 키에서 벗어나면 보간 버튼이 사라지고 안내 문구로 바뀐다
  - `0.30초 키 지우기` 버튼으로 지우면 트랙 목록이 비워진다
  - 콘솔 오류 없음
- 보간이 실제 계산에 반영되는지 단위 테스트로 확인
  - `일정하게`: 가운데가 정확히 절반
  - `뚝 끊어서`: 다음 키에 닿기 전까지 앞의 값 그대로
  - `부드럽게`: 가운데는 절반이되 앞쪽이 더 느리다

---

## v0.22.0 — 2026-08-16

**Phase**: 키프레임 편집 3단계 (키 옮기기 · 지우기) + 편집 흐름 보완

### 추가

- **찍은 키를 끌어서 옮긴다.** 타임라인의 밝은 마름모를 잡아 끌면 그 시각으로 옮겨진다.
  한 프레임(1/30초)에 맞춰 떨어지고, 한 시각의 키가 속성별로 함께 움직인다.
  드래그 한 번이 Undo 한 단위다.
- **키 지우기.** 마름모 우클릭, 또는 재생 헤드를 그 자리에 두고 `Delete`.
- **마름모를 세 갈래로 그린다.** 무엇을 손댈 수 있는지 겉보기로 알게 하려는 것이다.
  - 밝고 큼: **직접 찍은 키** — 끌고 지울 수 있다
  - 중간: 고른 관절이 움직이지만 **프리셋(태그)이 만든 키** — 잠겨 있다
  - 흐림: 다른 관절의 키
  - 안내도 `머리1: 내 키 1 / 전체 5`로 바꿨다
- **`ownKeyTimes` · `moveOwnKeys` · `removeOwnKeys`** (`core/animation/edit.ts`).
  모두 `{kind:"bone"}` Track만 건드린다 — 태그 Track을 여기서 옮기면 다른 관절까지 딸려 온다.
- **좌측 상단에 실행 취소 · 다시 실행 버튼.** 되돌릴 것이 없으면 흐려진다.

### 수정

- **Undo가 고르고 있던 관절을 놓아 버리던 것.** 한 획 되돌렸다고 붓을 내려놓게 되면
  이어서 칠할 수가 없다. 되돌린 뒤에도 남아 있는 관절이면 선택과 칠하기를 그대로 이어 간다.
- **`Ctrl+S` 등의 브라우저 기본 동작을 먼저 막는다.** 예전에는 저장을 시작한 **뒤에**
  `preventDefault`를 불러서, 그 사이에 무슨 일이 나면 브라우저가 페이지를 가져갈 수 있었다.
  이제 맨 앞에서 막고 그다음에 일한다. 이름을 고쳐 쓰는 중이면 `Ctrl+Z`는 글자 편집에 양보한다.
- **되돌릴 것이 없을 때도 알려 준다.** 예전에는 아무 반응이 없었다.

### 검증하다 고친 것

- **우클릭이 키 드래그까지 시작하던 것.** 타임라인 pointerdown이 버튼을 가리지 않아,
  우클릭으로 키를 지운 뒤 pointerup이 **삭제된 뒤의 상태를 히스토리에 한 번 더 쌓았다.**
  그래서 Undo를 눌러도 키가 되살아나지 않았다.
  히스토리 스냅샷을 직접 찍어 보고 원인을 잡았다. 왼쪽 버튼만 드래그를 시작하게 고쳤다.
- **집기만 하고 안 움직여도 히스토리가 쌓이던 것.** 실제로 옮겼을 때만 쌓는다.
- **상태줄이 거짓말하던 것.** 프리셋 키를 잡아 끌면 아무 일도 안 일어나는데
  "옮겼습니다"라고 했다. 실제로 바뀌었을 때만 알린다.

### 검증

- `npm test` 238개 통과 (새 10개), `npm run build` · `tsc --noEmit` 통과
- Chromium(Playwright)에서 `대기` 프리셋이 든 애니메이션으로 확인
  - 마름모: 내 키 1 · 프리셋 키 4 · 다른 관절 3 — 세 갈래로 갈린다
  - 키 위 커서 `grab`, 끌어서 `0.30초 → 1.13초`로 이동
  - 우클릭으로 삭제, `Undo` 버튼으로 `x@1.13 · y@1.13` 복구
  - Undo/Redo 버튼이 시작 시 꺼져 있다가 작업 후 켜진다
  - 칠하기를 켠 채 `Ctrl+Z` → 관절 선택과 붓이 그대로 유지된다
  - `Ctrl+S` → 저장되고 페이지는 그대로, 칠하기도 유지
  - 콘솔 오류 없음

---

## v0.21.0 — 2026-08-16

**Phase**: 키프레임 편집 2단계 (자세를 만들면 키가 찍힌다)

타임라인을 0.56초에 세워 두고 캔버스에서 관절을 움직이면 그 시각에 키가 찍힌다.
스파인이 하는 방식 그대로다.

### 추가

- **`core/animation/edit.ts` — 키 만들기 · 고치기.** 모두 원본을 건드리지 않는다.
  - `setKey` — 그 시각에 키를 놓는다. 있으면 값만 갈아 끼우고, Track이 없으면 만든다.
    아주 가까운 시각(1e-4초 이내)은 같은 키로 봐서 부동소수 오차로 키가 늘어나지 않는다
  - `removeKey` — 마지막 키를 지우면 Track째 없앤다. 키 없는 Track은 파일만 늘린다
  - `deltaWithoutOwnKeys` · `keyValueFor` — **적어야 할 값을 역산한다.**
    화면에서 원하는 만큼 움직이려면 태그 Track이 이미 주는 몫을 빼고,
    관절 `강도`와 애니메이션 `강도`로 나눠서 적어야 한다.
    이걸 빼지 않으면 프리셋이 있는 애니메이션에서 관절이 두 배로 움직인다
- **캔버스에서 자세 편집.** 애니메이션이 올라가 있고 멈춰 서 있을 때만 동작한다.
  - **관절점 안쪽을 끌면 이동** → 그 시각의 `x` · `y` 키.
    끌어 놓은 세계 좌표를 부모 자세의 역변환으로 지역 델타로 되돌려 적는다
  - **바깥 링을 끌면 회전** → 그 시각의 `rotation` 키.
    한 바퀴를 넘겨도 이어지도록 −π~π로 접어 누적한다
  - 키 시각은 한 프레임(1/30초)에 맞춰 떨어뜨린다
  - 드래그 한 번이 Undo 한 단위다

### 변경

- **관절점을 키웠다.** 반경 5 → 8px, 집기 판정 11 → 14px. 안쪽을 누르기 쉽도록.
- **선택된 관절에 회전 링을 그린다** (반경 26px, 집기 두께 ±9px).
  자세를 편집할 수 있을 때는 진하게, 아닐 때는 흐리게 그려 지금 되는지 알 수 있다.
  링 위에서는 커서가 `crosshair`로 바뀐다.

### 검증

- `npm test` 228개 통과 (새 17개), `npm run build` · `tsc --noEmit` 통과
- Chromium(Playwright)에서 `대기` 프리셋이 든 애니메이션으로 확인 (섞였을 때까지 본다)
  - 타임라인 0.557초에 세우고 `머리1` 선택 → 자세 편집 가능 상태
  - 관절점 위 커서 `grab`, 링 위 커서 `crosshair`
  - 안쪽을 화면상 (+40, −25)px 끌자 **`x` · `y` 키가 생기고**
    관절이 (110, 40.6) → (128.8, 28.3)으로 옮겨졌다 (줌 2.025 기준으로 정확)
  - 링을 90° 끌자 **`rotation` 키 1개**가 `0.57초 = 1.666rad`로 찍혔다
    (프리셋이 이미 주던 몫이 더해진 값)
  - 상태줄: `머리1: 0.57초(17F)에 키를 찍었습니다.`
  - 다른 시점으로 갔다 오면 자세가 달라진다 — 보간이 동작한다
  - `Ctrl+Z` 한 번에 키 23개 → 22개. 드래그 하나가 정확히 한 단위
  - 콘솔 오류 없음

### 검증하다 고친 것

- **드래그 중 Undo가 폭발하던 것.** 키를 쓸 때마다 `commit`을 불러
  마우스가 움직인 횟수만큼 히스토리가 쌓였다. `onDragStart`에서 한 번만 쌓고
  그 뒤로는 스토어만 갱신하도록 바꿨다.

### 다음

트랙 목록(어느 관절의 어느 속성에 키가 있는지)과 개별 키 삭제 · 보간 방식 편집.

---

## v0.20.0 — 2026-08-16

**Phase**: 키프레임 편집 1단계 (타임라인)

특정 시점에 세워 두는 것이 키프레임 편집의 전제다.
0.6초에서 팔을 돌리려면 먼저 0.6초에 설 수 있어야 한다.

### 추가

- **`AnimationPlayer`에 타임라인 조작.** (`core/animation`)
  - `seek(time)` — 재생 여부를 건드리지 않고 시각만 옮긴다. 범위 밖은 잘라 낸다
  - **옮기는 동안에는 이벤트를 내지 않는다.** 타임라인을 훑을 때마다 공격 판정이 울리면 안 된다
  - `pause()` / `resume()` — `stop()`과 달리 시각과 애니메이션이 남는다.
    끝에 서 있을 때 다시 누르면 처음부터 간다
  - `sample(bones)` — 시간을 굴리지 않고 지금 자세만 구한다
  - `time` · `duration` 읽기
- **`keyTimes(animation, bones, boneId?)`** — 키가 찍힌 시각들.
  관절을 주면 그 관절을 움직이는 Track만 본다. 태그로 잡는 Track도 포함된다 —
  화면에는 "이 관절이 이 시각에 움직인다"가 보여야 하기 때문이다.
- **`editor/ui/timeline.ts` — 타임라인 UI.** 하단 바 위에 제 줄을 갖는다.
  - 눌러서 그 시점으로, 끌어서 훑어보기
  - 0.1초 눈금(길이에 따라 자동으로 벌어진다)과 1초마다 숫자
  - 키가 찍힌 시각을 마름모로. 고른 관절의 키는 더 크고 밝게
  - `0.36 / 0.80초 · 11F` 식으로 시각과 프레임 번호를 함께 보여 준다
  - 재생 헤드는 삼각형 + 세로선
- **단축키.** `Space` 재생/일시정지, `←` `→` 한 프레임(`Shift`면 5프레임), `Home` 처음으로.
  입력칸에 focus가 있을 때는 동작하지 않는다.

### 참고

- 정지 상태에서 눈금을 눌러도 그 시점에 선다. 고른 애니메이션을 멈춘 채로 올린다.
- 타임라인이 나타나면 애니메이션 설정 패널은 그만큼 위로 올라간다.

### 검증

- `npm test` 212개 통과 (새 12개), `npm run build` · `tsc --noEmit` 통과
- Chromium(Playwright)에서 확인
  - 이미지 전에는 감춰져 있고, 애니메이션을 넣으면 나타난다
  - 눈금 9개 · 키 마름모 6개가 `이동`(0.8초)에서 그려진다
  - 25% 클릭 → 0.197초, 75% → 0.603초. 읽기와 헤드 위치가 함께 맞는다
  - 끌어서 훑기 동작. 정지 상태에서 눌러도 0.359초에 선다
  - `→` 한 번에 33ms(1/30초), `Home`으로 0초
  - `다리1`을 고르면 그 관절의 키 3개가 강조된다 (0 · 0.4 · 0.8초)
  - 시점마다 캔버스 픽셀이 달라지고, `중심1`의 y가
    **250 → 248 → 246 → 248 → 250**으로 걷기 리듬을 그린다
  - `Space`로 재생 토글
  - 콘솔 오류 없음

### 다음

이 시점에서 관절을 돌리면 키가 찍히는 편집 → 트랙 목록과 보간 방식 편집.

---

## v0.19.1 — 2026-08-16

**Phase**: 키프레임 편집 준비 0단계 (관절점이 자세를 따라간다)

재생하면 그림은 휘는데 **관절점과 연결선은 기준 자세에 박혀 있었다.**
지금까지는 재생이 확인용 미리보기였고 편집은 멈춘 상태에서만 해서 문제가 없었지만,
키프레임 편집을 넣으면 "0.6초 시점에서 팔을 잡아 돌리기"가 불가능해진다.

### 수정

- **관절점 · 연결선이 재생 중인 자세를 따라간다.**
  `drawBonePoints` · `drawLinks`가 `bone.x, bone.y`(기준 좌표)를 그대로 쓰고 있었다.
  `EditorScene.setPosedBones()`로 매 프레임 자세를 넘기고, 그 자리에 그린다.
- **관절 집기도 같은 자리를 쓴다.** 그려진 곳과 잡히는 곳이 어긋나면 안 된다.
- **재생 중에는 관절을 옮길 수 없게 했다.** 화면의 점은 자세를 따라가 있으므로
  그 상태에서 끌면 기준 좌표가 엉뚱한 곳으로 간다. 상태줄로 알려 준다.
- 정지하면 `setPosedBones(null)`로 기준 자세로 되돌아온다.

### 검증

- `npm test` 200개 통과, `npm run build` · `tsc --noEmit` 통과
- Chromium(Playwright)에서 `점프`를 20배 느리게 재생하며 네 시점을 재 봤다

  | 재생 시각 | 고치기 전 (그려진 y) | 고친 뒤 (그려진 y) | 실제 자세 y |
  | --- | --- | --- | --- |
  | 0.50 | 46 | 46 | 46 |
  | 0.56 | 46 | 47 | 47 |
  | 0.61 | 46 | 49 | 49 |
  | 0.67 | 46 | 53 | 53 |

  고치기 전에는 46에 고정이었고, 지금은 실제 자세와 **모든 시점에서 0px 차이**다.
- 눌린 몸 위에 관절점과 연결선이 얹혀 있는 것을 스크린샷으로 확인
- 정지하면 `posed`가 `null`로 돌아가 기준 자세로 복귀
- 콘솔 오류 없음

### 다음

타임라인(재생 헤드 · 키프레임 표시) → 그 시점에서 관절을 돌리면 키가 찍히는 편집 →
트랙 목록과 보간 방식 편집 순으로 간다.

---

## v0.19.0 — 2026-08-16

**Phase**: 기획서 37 (로컬 자동 저장)

탭을 닫으면 작업이 통째로 날아갔다. 저장 파일을 만들기 전까지는 아무 안전망이 없었다.

### 추가

- **`editor/tools/autosave.ts` — IndexedDB 자동 저장.** (기획서 37)
  - 손을 멈추고 1.2초 뒤에 한 번 담는다. 관절을 끌거나 칠하는 동안에는 담지 않는다
  - 탭을 닫을 때(`pagehide` · `visibilitychange`) 예약된 것을 마저 담는다
  - 저장 칸은 하나다. 여러 프로젝트를 들고 다니는 것은 파일이 할 일이다
  - 의존성을 늘리지 않으려고 IndexedDB를 직접 쓴다 (기획서 63)
  - 저장에 실패해도 편집을 막지 않고 다음 변경 때 다시 시도한다
  - 담아 둔 내용이 지금 포맷과 맞지 않으면 조용히 버린다.
    복구하려다 편집기가 열리지 않는 편이 더 나쁘다
- **다시 열면 이어서 시작한다.** 이미지 · 관절 · 칠한 영향 영역 · 애니메이션이 그대로 살아나고,
  상태줄에 "방금 작업을 이어서 엽니다"와 새로 시작하는 법을 적어 준다.
- **`파일 → 새 프로젝트`가 담아 둔 것도 지운다.** 안 그러면 새로 시작해도 다음 새로고침에
  옛 작업이 되살아난다.

### 참고

- 이미지는 매번 다시 담지 않는다. 마지막으로 담은 Object URL과 지금 것을 비교해
  달라졌을 때만 PNG를 넣는다. 관절 하나 옮길 때마다 이미지를 다시 쓰지 않기 위한 것이다.
- 자동 저장은 저장 파일(`.puppet.zip`)을 대신하지 않는다.
  다른 기기로 옮기거나 백업하려면 여전히 `파일 → 프로젝트 저장`이 필요하다.

### 검증

- `npm test` 200개 통과 (새 9개), `npm run build` · `tsc --noEmit` 통과
- Chromium(Playwright)에서 실제 새로고침으로 확인
  - 이미지 + 관절 4개 + 칠하기 + `이동` 추가 후 새로고침
  - IndexedDB에 JSON 31,473자 · 이미지 5,763바이트가 담김
  - 새로고침 뒤 이름 · 관절 4개 · 애니메이션 2개 · 칠해진 정점 152개 ·
    이미지까지 **작업 직전과 완전히 동일** (스냅샷 비교 일치)
  - 좌측 관절 목록과 캔버스 그림도 그대로 복원됨
  - `새 프로젝트` → 저장소가 비워지고, 새로고침해도 빈 상태로 열림
  - 콘솔 오류 없음

### 검증하다 고친 것

- **이미지가 안 담기던 것.** 처음에는 "이미지가 바뀌었다" 표시를 플래그로 뒀는데,
  그 플래그를 켜는 시점이 스토어 갱신보다 늦어서 저장이 매번 이미지를 건너뛰었다.
  플래그 대신 마지막으로 담은 Object URL과 지금 것을 비교하도록 바꿨다.

---

## v0.18.1 — 2026-08-16

**Phase**: 배포 후 정리

npm에 올린 뒤 패키지 페이지를 보니, 첫 화면이 편집기 개발 안내(`npm install` → `npm run dev`)로
시작해서 **라이브러리 쓰는 법으로 읽히지 않았다.** 사람도 AI도 헷갈릴 자리였다.

### 변경

- **README를 소비자 기준으로 다시 세웠다.**
  - 머리말에서 편집기 · 런타임 두 부분임을 표로 먼저 밝힌다
  - `npm install puppetforge`와 사용 예시를 맨 위로 올렸다
  - 편집기 개발 안내(`npm run dev`)는 `편집기를 직접 돌리기`로 이름을 바꿔 아래로 내렸다.
    라이브러리 설치와 헷갈리지 않게 하기 위한 것이다
  - 배포 전에 적어 둔 GitHub 설치 대안과 배포 방법 안내는 지웠다

### 검증

- npm에 `puppetforge@0.18.0` 공개 (MIT · 의존성 0 · 32.5 kB · 22개 파일)
- 빈 프로젝트에서 `npm install puppetforge`로 실제 레지스트리에서 받아 재생 확인
  - 애니메이션 4개 인식, `walk`에서 정점이 최대 25.46px 이동, `step` 이벤트 수신
  - 없는 이름 재생은 오류 없이 `false`
- 배포에 쓴 토큰 두 개 모두 폐기 확인 (`npm whoami` → E401)

---

## v0.18.0 — 2026-08-16

**Phase**: Phase 10 (runtime-phaser) — 마지막 Phase

내보낸 파일이 실제 게임에서 도는 것을 눈으로 확인했다.
지금까지 만든 `stagger` · `focus` · 애니메이션별 `deform`이 편집기 밖에서도
그대로 살아 있는지는 이번에 처음 검증됐다.

### 추가

- **`src/runtime/phaser.ts` — `PuppetCreature`.** (기획서 41, 43)
  - `PuppetCreature.load(scene, url | bytes, { x, y, play, key })` — 읽고 텍스처를 올리고
    씬에 붙이는 것까지 한 번에 한다. `scene.add.existing`을 따로 부를 필요가 없다
  - `Phaser.GameObjects.Mesh`를 상속하므로 `setPosition` · `setScale` · `setDepth` 같은
    보통의 조작이 그대로 통한다
  - `play(name)` · `stopAnimation()` · `setAnimationSpeed()` · `setAnimationStrength()`
  - `setFlipX(true)` — 좌우 뒤집기. 삼각형 감김이 뒤집히므로 `hideCCW`를 꺼 둔다
  - `onEvent(name | "*", fn)` — 애니메이션 이벤트 (기획서 42)
  - `poseAt(name, time)` — 재생하지 않고 한 자세로 세워 둔다
  - `core`로 밑의 `Puppet`에 바로 닿는다
  - 씬의 `update`에 스스로 붙는다. 매 프레임 부를 것이 없다
  - 같은 캐릭터를 여러 마리 세워도 텍스처는 한 번만 올라간다
- **`example/` — 동작하는 예제 페이지.** 편집기 코드를 하나도 쓰지 않는다.
  내보낸 zip을 열면 애니메이션 목록이 버튼으로 뜨고, 눌러서 전환하고 좌우로 뒤집는다.
  `npm run dev` 후 `/example/`에서 볼 수 있고, 배포에도 함께 올라간다.
- **패키지에 `puppetforge/phaser` 서브패스.** Phaser는 `optional` peerDependency다 —
  Phaser를 안 쓰면 없어도 되고, 쓰면 게임이 이미 들고 있는 것을 쓴다.

### 검증

- `npm test` 191개 통과, `npm run build` · `tsc --noEmit` 통과
- Chromium(Playwright)에서 예제 페이지로 확인 (편집기와 완전히 분리된 페이지)
  - 편집기가 내보낸 zip을 열어 캐릭터 `human` 220×320, 정점 9504개로 세워짐
  - 애니메이션 버튼 4개(`idle` `idle2` `walk` `attack`)가 파일에서 채워짐
  - `대기` 재생 중 캔버스 프레임 4장이 모두 다름
  - `walk` 버튼으로 전환되고 프레임 3장이 모두 다름
  - `좌우 뒤집기` → `scaleX -1.000`, 화면 픽셀도 바뀜
  - 없는 이름 재생 → `false` (오류 없음)
  - `step` 이벤트가 화면에 표시됨
  - 콘솔 오류 없음
- 소비자 쪽에서 `import { PuppetCreature } from "puppetforge/phaser"`가
  타입까지 해석되는 것을 `npm pack` → 설치 → `tsc`로 확인 (종료 코드 0)
- 번들 크기: `dist-lib/phaser.js` 4.34 kB (gzip 1.99 kB). Phaser 자체는 번들에 들어가지 않는다

### 참고

- Phaser 어댑터는 WebGL과 DOM이 있어야 해서 Vitest(node) 단위 테스트를 붙이지 않았다.
  대신 실제 브라우저에서 픽셀로 확인한다.
- 이로써 기획서 70의 Phase 1~11이 모두 끝났다.

---

## v0.17.0 — 2026-08-16

**Phase**: Phase 9 (runtime-core) · npm 패키지 준비

게임에 넣을 라이브러리를 떼어 냈다. 편집기 코드는 하나도 들어가지 않고,
렌더링 엔진도 모른다 — "이 순간 정점이 어디 있어야 하는가"까지만 계산한다.

### 추가

- **`src/runtime/index.ts` — Runtime Core.** (기획서 40, 43)
  - `Puppet.load(url | bytes)` — 내보낸 zip이나 puppet.json을 읽는다.
    URL이면 fetch하고, ZIP이 아니면(`PK`로 시작하지 않으면) JSON으로 본다
  - `.play(name, { speed, strength, restart })` — 없는 이름이면 오류 없이 `false`.
    캐릭터마다 가진 동작이 다르므로 없으면 그냥 안 하는 편이 낫다 (기획서 64)
  - `.stop()` · `.setSpeed()` · `.setStrength()`
  - `.on(event, fn)` · `.on("*", fn)` — 애니메이션 이벤트. 돌려받은 함수로 해제 (기획서 42)
  - `.update(dt)` — 시간을 굴리고 변형된 정점(Float32Array). 멈춰 있으면 `null`.
    같은 배열을 다시 쓴다
  - `.poseAt(name, time)` — 재생하지 않고 한 자세의 정점만
  - `.animations` · `.uv` · `.restVertices` · `.texture` · `.mesh` · `.pixelArt`
  - 편집기와 똑같이 애니메이션 → 따라 흔들림 → 스키닝 순서로 계산하고,
    애니메이션별 `deform` 덮어쓰기도 그대로 적용한다
- **npm 패키지 설정.** `puppetforge`라는 이름이 비어 있어 그대로 쓴다.
  - `npm run build:lib` — `vite.lib.config.ts`로 번들, `tsconfig.lib.json`으로 타입 선언
  - `exports` · `types` · `files`로 `dist-lib`만 올라가게 했다. 편집기는 들어가지 않는다
  - `prepublishOnly`에 테스트와 빌드를 걸어 두었다
  - Phaser를 `dependencies`에서 `devDependencies`로 옮겼다.
    편집기 앱만 쓰는 것이라 라이브러리를 설치하는 쪽에 딸려 가면 안 된다
  - MIT 라이선스와 LICENSE 파일을 넣었다 — **다른 라이선스를 원하면 알려 달라**
- **내보내기에 `_readme` 한 줄.** (Puppet JSON v11)
  파일을 열자마자 무엇이고 어떻게 재생하는지 보이도록 맨 앞에 둔다.
  바이브 코딩할 때 AI가 파일만 보고 바로 작업할 수 있게 하려는 것이다.
  읽는 쪽은 무시하고, 편집기가 다시 읽을 때도 버린다. 저장 파일에는 붙지 않는다.

### 검증

- `npm test` 191개 통과 (새 19개), `npm run build` · `tsc --noEmit` 통과
- **실제로 설치해서 써 봤다.** `npm pack`으로 만든 tgz를 빈 프로젝트에 설치하고
  `import { Puppet } from "puppetforge"`로 불러 편집기에서 내보낸 zip을 재생
  - 이름 `human` 220×320 · 애니메이션 4개 · 텍스처 `human.png` 5763바이트 · 정점 1666개
  - 없는 이름 재생 → `false` (오류 없음), `walk` 재생 → 정점이 최대 25.46px 이동
  - `step` 이벤트 수신, `stop()` 뒤 `update`는 `null`, `poseAt`는 1666개 정점
  - 소비자 쪽 `tsc`로 타입이 해석되는 것까지 확인 (종료 코드 0)
- 패키지 크기: tgz 29.4 KB, 번들 `dist-lib/index.js` 26.9 KB (gzip 9.7 KB).
  몬스터를 몇 개 만들든 이 코드는 한 벌만 들어간다
- `_readme`가 내보낸 파일 맨 앞 키로 들어가고, 저장 파일에는 없다

### 수정

- **GitHub에서 설치하면 빈 패키지가 되던 것.** `dist-lib`은 git에 넣지 않으므로
  `npm i github:Bell7080/WebGLE`로 받으면 빌드 결과가 없었다.
  npm이 git 의존성에서 자동으로 부르는 `prepare`에 빌드를 걸어 해결했다.
  `git+file://`로 실제 설치해 재생까지 확인했다.

### 알려진 한계

- Phaser 어댑터(`puppetforge/phaser`의 `PuppetCreature`)는 아직 없다. Phase 10이다.
  지금은 `update(dt)`가 준 정점을 직접 렌더러에 넘겨야 한다.
- 아직 npm 레지스트리에 올리지 않았다. `npm publish`는 npm 계정 로그인이 필요해
  직접 실행해야 한다. 그 전에도 GitHub 주소로 설치하면 똑같이 쓸 수 있다.

---

## v0.16.0 — 2026-08-16

**Phase**: Phase 9 준비 (내보내기 슬림화)

Runtime을 만들기 전에 파일부터 줄였다. 런타임이 파일 읽는 코드를 새로 쓰므로,
"격자는 재생성한다"를 전제로 짜는 편이 나중에 두 번 고치지 않는 길이다.

내보낸 zip 하나가 371KB였는데, 뜯어보니 내용의 90%가 사람이 만든 값이 아니라
기계가 계산한 격자였다. 몬스터 10마리면 3.7MB가 된다.

| 항목 | 크기 | |
| --- | --- | --- |
| mesh.vertices | 57.7 KB | 이미지 크기로 다시 만들 수 있음 |
| mesh.indices | 45.9 KB | 이미지 크기로 다시 만들 수 있음 |
| mesh.weights | 63.0 KB | 1862개 중 1089개가 빈 값 |
| bones | 0.4 KB | 실제로 만든 내용 |
| animations | 3.4 KB | 실제로 만든 내용 |

### 추가

- **`core/format/compact.ts`** — 파일에 적을 때 Mesh를 줄이고 읽을 때 되살린다. (Puppet JSON v10)
  - `compactMesh` / `expandMesh` / `isCompactMesh`
  - `vertices` · `indices`를 빼고, 칠하지 않은 정점은 `null` 하나로 접는다
  - 되살릴 때 이미지 크기와 해상도로 `createGridMesh`를 다시 부른다
  - 정점 수가 맞지 않으면(이미지가 바뀐 경우) 가중치를 버리고 빈 격자를 준다.
    억지로 끼워 맞춰 엉뚱한 곳이 휘는 것보다 낫다

### 변경

- **`serializeProject(project, pretty = true)`** — 항상 줄여서 적는다.
  `pretty`를 끄면 들여쓰기도 뺀다. 게임에 넘길 내보내기에서 끈다.
- **`parseProject`** — 격자가 없는 파일이면 되살리고, 있는 파일(v9 이하)은 손대지 않는다.
- 메모리 안의 `PuppetMesh`는 그대로다. 파일에 오갈 때만 모양이 달라지므로
  편집기 · 렌더러 코드는 건드리지 않았다.

### 참고

- 게임에 들어갈 런타임 계산 로직만 따로 묶어 재 보니 **6.3 KB(gzip)**다.
  몬스터를 몇 개 만들든 이 코드는 한 벌만 들어간다. Phaser가 1.2MB쯤이다.
- 편집기 저장 파일(`.puppet.zip`)도 같이 작아진다. 다만 들여쓰기는 남겨 사람이 읽을 수 있게 둔다.

### 검증

- `npm test` 172개 통과 (새 11개), `npm run build` · `tsc --noEmit` 통과
- 격자 재생성이 정확한지: 내보낸 파일의 격자와 `createGridMesh(200, 260, "normal")` 결과가
  좌표 3,724개와 인덱스 10,656개까지 완전히 일치
- Chromium(Playwright)에서 관절 7개 · 애니메이션 4개짜리 사람 모양으로 확인 (220×320)
  - 같은 캐릭터 기준 **264.0 KB → 38.2 KB** (6.9배). zip으로는 44.0 KB
  - 편집기 저장 파일 94.3 KB
  - 정점 1666개 중 1283개가 `null`로 접힘
  - 내보낸 파일을 다시 불러오니 정점 3332 · 인덱스 9504 · 칠해진 정점 383개가
    내보내기 전과 완전히 같고, 관절 7개 · 애니메이션 4개도 그대로
  - 다시 읽은 캐릭터로 `이동`을 재생하니 캔버스 프레임 3장이 모두 다름 (실제로 움직인다)
  - 콘솔 오류 없음
- 하위 호환: 격자가 들어 있는 v9 이하 파일은 그 격자를 그대로 쓴다.
  이미지 크기가 달라져 있어도 다시 만들지 않는다.

---

## v0.15.1 — 2026-08-15

**Phase**: Phase 6 다듬기 (하단 바 정리)

애니메이션이 많아지면 같은 줄에 있던 속도 · 강도 · 흔들림 막대와 `되돌리기`가
목록에 밀려 찌그러졌다.

### 변경

- **애니메이션 설정을 하단 바 위 레이어로 뺐다.** 목록이 몇 개든 막대는 76px,
  `되돌리기`는 64px 그대로다. 하단 바 오른쪽 위에 떠 있으며 프리셋 목록(왼쪽)과 겹치지 않는다.
- **목록에 조절 대상 이름을 붙였다.** 목록에서 떨어져 떠 있게 되어 어느 애니메이션의
  값인지 보이지 않으면 헷갈리기 때문이다.
- **목록이 길면 가로로 스크롤된다.** 재생 · 정지 버튼을 화면 밖으로 밀어내지 않는다.

### 수정

- **하단 바가 화면 폭을 넘어 늘어나던 것.** 그리드 항목의 기본 최소 폭(`min-width: auto`) 때문에
  목록이 길어진 만큼 바 자체가 늘어나 재생 버튼과 설정이 화면 밖으로 나갔다.
  프리셋 13종을 모두 넣으면 1280px 화면에서 바가 1391px이 됐다.

### 검증

- `npm test` 161개 통과, `npm run build` · `tsc --noEmit` 통과
- Chromium(Playwright)에서 프리셋 13종을 모두 넣고 확인 (1280×760)
  - 하단 바 1391px → **1280px** (화면 폭에 맞음)
  - 막대 세 개 모두 76px · `되돌리기` 64px — 찌그러짐 없음
  - 설정이 하단 바 위에 있고 화면 안에 들어옴, 재생 버튼도 화면 안
  - 목록이 가로 스크롤됨 (1224 > 1113), 재생 버튼을 밀지 않음
  - 막대를 끌면 값이 바뀌고(1.00배 → 2.55배), 다른 애니메이션을 고르면
    대상 이름과 값이 따라 바뀐다
  - 프리셋 목록을 펼쳐도(462px, 왼쪽) 설정과 겹치지 않음
  - 콘솔 오류 없음

---

## v0.15.0 — 2026-08-15

**Phase**: Phase 6 보강 (애니메이션별 변형 방식)

대기에서 발을 바닥에 묶으려고 변형을 `위치 고정`으로 바꾸면 공격 · 이동까지 발이 묶였다.
변형이 Bone 속성이라 모든 애니메이션이 하나를 나눠 쓰고 있었기 때문이다.

### 추가

- **애니메이션에 `deform`.** 관절별 변형 방식을 그 애니메이션에서만 덮어쓴다. (Puppet JSON v9)
  - Bone의 `deform`은 그대로 **모든 애니메이션 공용 값**이고, 적어 둔 관절만 덮어쓴다
  - 지금 없는 관절의 덮어쓰기는 조용히 무시한다 (기획서 64).
    관절을 지우면 각 애니메이션의 덮어쓰기도 함께 지운다
- **속성 패널 `변형`에 범위 탭.** 하단에서 애니메이션을 고르면 `공용` / `<이름>에서만` 둘이 나온다.
  - `…에서만` 탭은 공용 값에서 시작한다. 아직 덮어쓰지 않은 값은 점선 테두리로 "따르는 중"임을 보여 준다
  - 하나라도 덮어쓰면 `공용으로 되돌리기`가 생겨 언제든 되돌릴 수 있다
  - 애니메이션을 고르지 않았으면 탭 없이 예전 그대로 공용 값만 편집한다
- **`deformModesFor(bones, animation)`** (`core/animation`). 재생할 애니메이션에 맞는
  관절별 변형 방식을 만든다. 런타임도 이 함수 하나만 쓰면 된다.

### 검증

- `npm test` 161개 통과 (새 6개), `npm run build` · `tsc --noEmit` 통과
- Chromium(Playwright)에서 다리 · 발이 있는 사람 모양으로 확인
  - `발1`을 고르고 `idle에서만` 탭 → `위치 고정`
  - 저장된 값: 공용 `soft` · idle `pinnedSoft` · attack `(없음)`
  - 발1이 칠해진 정점 34개의 최대 이동: **idle 1.06px · attack 7.47px**
    (대기에서는 발이 붙어 있고 공격에서는 그대로 움직인다)
  - 안내 문구가 "공용 값을 따르는 중" → "idle에서만 따로 정해져 있습니다"로 바뀌고
    `공용으로 되돌리기`가 생긴다
  - 탭 · 되돌리기 버튼이 우측 패널 안쪽에 들어온다 (넘침 0px)
  - 콘솔 오류 없음
- 저장 · 불러오기 왕복에서 `deform`이 그대로 남는다
- 하위 호환: `deform`이 없는 기존 파일은 예전처럼 Bone의 공용 값만 쓴다

---

## v0.14.0 — 2026-08-15

**Phase**: Phase 6 보강 (동작의 주인공 고르기)

손이 여섯인데 하나만 검을 쥔 경우를 만들어 재 보니, 여섯 팔이 똑같이 휘둘러졌다.
검 쥔 팔(1.29 rad)이 맨손 팔(0.39 rad)보다 크게 움직이도록 고쳤다.

### 추가

- **Track에 `focus` · `focusOther`.** 이 동작의 주인공을 태그로 고른다. (Puppet JSON v8)
  - 대상 중 **자기 자신이나 후손이** `focus` 태그를 가진 쪽만 값을 그대로 받고,
    나머지는 `focusOther` 배(기본 0.3)만 받아 거드는 정도로 움직인다
  - 팔 → 손 → 검처럼 몇 단계 아래에 달려 있어도 팔까지 올라온다.
    검을 팔에 직접 붙이지 않아도 된다
  - 대상 중 아무도 해당하지 않으면 가리지 않는다. 무기가 없는 캐릭터가 맨손 공격을
    못 하게 되면 안 되기 때문이다 (기획서 64). 전부 해당할 때도 마찬가지다
- **`bonesCarrying(bones, tag)`** (`core/skeleton`). 자기 자신이나 후손이 그 태그를 가진
  관절의 id 집합을 돌려준다. "무기를 쥔 팔이 어느 쪽인가"를 이름 없이 알아내기 위한 것이다.
  순환 참조가 있어도 멈추지 않는다.

### 변경

- **`attack` 태그를 대상으로 하는 Track에 `focus: "weapon"`.**
  `공격` · `휘두르기` · `찌르기` · `할퀴기` 넷이다.
  `몸통박치기` · `캐스팅` · `물기`는 애초에 `attack`을 안 써서 그대로 두었다.

### 참고

- 전갈처럼 집게도 독침도 전부 `weapon`이면 가리지 않고 다 쓴다.
  독침만 쓰게 하려면 집게에서 `weapon`을 떼면 된다.
- 손 자체를 대상으로 하는 프리셋은 아직 없다. 손은 팔을 따라 움직인다.

### 검증

- `npm test` 155개 통과 (새 9개), `npm run build` · `tsc --noEmit` 통과
- 팔 여섯 중 하나만 검을 쥔 몬스터의 최대 회전(rad)

  | 프리셋 | 검 쥔 팔 | 맨손 팔 다섯 |
  | --- | --- | --- |
  | 휘두르기 | 1.29 | 0.39 |
  | 공격 | 0.74 | 0.22 |
  | 할퀴기 | 0.68 | 0.20 |

  맨손 다섯은 서로 완전히 같은 값이고, 0은 아니다(멈추지 않고 거든다).
- Chromium(Playwright)에서 관절 11개짜리 육수(六手) 몬스터를 실제로 만들어 확인
  - 앱 안에서 잰 회전값도 `팔2: 1.29 / 나머지: 0.387`로 같다
  - 정지 자세 대비 바뀐 픽셀이 무장한 오른쪽 30,894 · 맨손 왼쪽 16,994
  - 스크린샷에서 검 쥔 팔만 머리 위로 넘어가고 나머지는 살짝 기운다
  - 콘솔 오류 없음
- 하위 호환: `focus`가 없는 기존 파일은 예전과 똑같이 전부 함께 움직인다

### 검증하다 겪은 것

- 한 번짜리 애니메이션(`휘두르기`, 1초)은 swiftshader에서 스크린샷 한 장에 ~750ms가 걸려
  촬영 사이에 끝나 버렸다. 처음엔 "변형이 안 된다"로 보였지만 실제로는 촬영 타이밍 문제였다.
  `player.setSpeed(0.05)`로 20배 늦춰 잡으니 프레임 5장이 모두 다르게 나왔다.
  반복 애니메이션(`이동`)은 이 문제가 없다.

---

## v0.13.0 — 2026-08-15

**Phase**: Phase 6 보강 (여러 개인 부위의 위상)

생김새가 제각각인 몬스터 일곱을 실제로 만들어 프리셋을 돌려 보고, 거기서 나온 구멍을 메웠다.
검사한 것: 팔4·다리4 / 뱀(팔다리 없음) / 유령(다리 없음) / 지네(다리 20) / 전갈(꼬리 독침) /
도적(단검) / 팔척귀신(긴 팔).

### 추가

- **Track에 `stagger`.** 한 Track이 여러 관절을 잡을 때 시간을 얼마나 벌릴지 정한다.
  n개 중 i번째는 `time + duration × stagger × i / n`의 값을 쓰고, 순서는 관절 목록 순서다.
  없으면 0이라 지금까지처럼 모두 동시에 움직인다. (Puppet JSON v7)
  - 이전에는 태그가 같으면 **완전히 같은 값**을 받아서, 다리가 둘이든 스물이든 한 몸처럼 굴렀다.
    사람은 제자리 뛰기로 보였고 지네는 다리 스무 개가 동시에 붙었다 떨어졌다
  - 반복 애니메이션은 주기를 감아 이어 붙이고, 한 번짜리는 끝에서 멈춘다
- **`독침` 파츠.** 태그는 `attack` · `weapon` · `stiff`.
  꼬리를 부모로 두면 꼬리째 휘둘러 찌른다. 전갈 · 벌처럼 꼬리로 공격하는 몬스터용이다.
- **`tests/creatures.test.ts` · `tests/creatures.fixture.ts`** — 위 일곱 몬스터를 관절 트리로 만들어
  프리셋마다 모든 관절이 실제로 움직이는지, 어긋냄이 제대로 갈리는지 확인한다. 16개.

### 변경

- **`walk` 프리셋의 `leg` · `arm` Track에 `stagger: 1`.**
  다리 둘은 정확히 반대로(+0.22 / −0.22), 넷은 주기를 넷으로 나눠, 스물은 파도로 나간다.
  `attack` 계열은 그대로 두었다 — 팔이 넷이면 넷으로 한꺼번에 내려치는 편이 낫다.

### 참고

- 태그를 안 쓰는 부위도 계층을 타고 함께 움직인다. 도적의 단검은 `swing`에서 127px 움직이는데,
  `weapon` Track(53px)에 팔 · 손의 회전이 얹힌 결과다. 전갈 독침도 꼬리를 타고 69px 간다.
- 여전히 수동인 것: 어느 다리가 앞이고 뒤인지는 관절 목록 순서가 정한다.
  좌측 패널에서 끌어 정리하면 파도 방향이 바뀐다.

### 검증

- `npm test` 146개 통과 (새 16개 포함), `npm run build` 통과
- 어긋냄 실측 (walk, t=0의 `rotation`)
  - 다리 2개: `0.22 / -0.22` — 정확히 반대
  - 다리 4개: `0.22 / 0.00 / -0.22 / 0.00` — 네 위상
  - 다리 20개: `0.22 0.21 0.17 0.12 … -0.22 … 0.21` — 이웃 간 차이는 0.1 미만으로 이어지고
    전체로는 앞뒤가 반대까지 간다. t가 지나면 파형이 몸을 타고 흘러간다
- Chromium(Playwright)에서 다리 20개짜리 지네를 실제로 만들어 확인
  - 관절 23개를 UI로 추가 → 영향 영역 칠하기 → `이동` 재생
  - 주기를 넷으로 나눈 캔버스 스크린샷 4장이 모두 다르고, 다리 각도가 몸을 따라 다르게 잡힌다
  - 콘솔 오류 없음
- 하위 호환: `stagger`가 없는 기존 파일은 0으로 읽혀 예전과 똑같이 동작한다

---

## v0.12.1 — 2026-08-15

**Phase**: Phase 5 · 7 다듬기 (상단 메뉴 정리)

### 변경

- **상단 파일 버튼 다섯 개를 `파일` 하나로 모았다.** `새 프로젝트` · `이미지 불러오기` ·
  `프로젝트 열기` · `프로젝트 저장`(Ctrl+S) · `내보내기`가 눌러서 펼치는 목록으로 들어갔다.
  상단이 캔버스를 덜 잡아먹고, 늘어난 기능만큼 버튼이 계속 늘어나지 않는다. (기획서 74)
- **`설정` 버튼을 다시 만들었다.** v0.12.0에서 없앤 뒤 캐릭터 설정을 속성 패널에 넣었는데,
  관절 선택을 풀어야만 보이는 자리라 찾기 어려웠다. 상단에서 바로 연다.
  - `이름` · `그림`(일반 / 도트) · `격자`(낮음 · 보통 · 높음)
  - 도트는 여전히 불러올 때 자동으로 판정하고, 그 근거를 패널에 적어 둔다.
    자동 판정이 틀렸을 때 여기서 수동으로 덮어쓴다
  - 격자를 바꾸면 새 격자의 칸 수 · 정점 수가 바로 갱신된다
- **속성 패널의 캐릭터 설정은 한 줄 요약 + `설정 열기` 버튼으로 바꿨다.**
  이름 · 크기 · 그림 종류 · 격자 · 관절 수 · 애니메이션 수를 한 줄로 보여 준다.

### 수정

- 설정의 이름 칸에서 포커스가 빠질 때 패널을 그 자리에서 갈아 끼우다
  `replaceChildren` 오류가 나던 것. 다시 그리기를 다음 차례로 미룬다.

### 참고

- 격자 해상도는 변형용 격자의 촘촘함이다. 영향 영역을 보여 주는 점 패턴 크기와는 무관하다.
  점 패턴은 화면 표시용 고정 간격이라 격자를 바꿔도 그대로다.

### 검증

- `npm test` 130개 통과, `npm run build` 통과
- Chromium(Playwright)에서 실제 확인
  - 상단 버튼이 `파일` · `설정` 둘뿐. 파일 목록 5개 모두 표시되고 `프로젝트 저장`에 `Ctrl+S` 표기
  - 바깥 클릭 · `Esc`로 닫힘, 저장을 누르면 메뉴가 닫히고 다운로드가 시작됨
  - 이미지 전에는 "이미지를 불러오면 설정할 수 있습니다."만 나옴
  - 격자 `높음` → `37×48칸`이 `55×72칸 · 정점 4088개`로 바뀌고 패널은 열린 채 유지
  - `도트` → 상태줄 "도트 모드로 그립니다.", 이름 `거미`로 변경 반영
  - 콘솔 오류 없음

---

## v0.12.0 — 2026-08-15

**Phase**: Phase 5 · 7 다듬기 (도트 자동 판정 · 캐릭터 설정)

### 추가

- **도트 그림 자동 판정** (`core/image/pixelart.ts`). 이미지를 불러올 때 픽셀을 보고 스스로 정한다.
  사용자에게 묻지 않는다. (기획서 51, 73)
  - 같은 색 블록이 있으면 확대해 저장한 도트로 본다 (`4배로 확대된 도트 그림으로 보입니다`)
  - 블록이 없어도 경계가 또렷하고 · 색이 64가지 이하이고 · 색 경계가 섞이지 않았으면 도트로 본다
  - 도트로 판정되면 격자도 자동으로 `낮음`이 된다. 과하게 휘면 픽셀이 깨져 보이기 때문이다
  - 판정 근거를 상태줄과 설정 패널에 그대로 보여 준다
- **캐릭터 설정.** 관절을 고르지 않았을 때 우측 패널에 나온다.
  - `이름` — 저장 · 내보내기 파일명이 여기서 나온다. 지금까지는 바꿀 방법이 없었다
  - `그림` — 일반 / 도트. 자동 판정이 틀렸을 때 직접 바꾼다
  - `격자` — 낮음 · 보통 · 높음 (기획서 15). 지금까지 `보통` 고정이었다
- **격자를 바꿔도 칠해 둔 영향 영역이 남는다.** 새 격자의 각 정점 값을 옛 격자에서
  이중 선형 보간으로 읽어 옮긴다 (`resampleWeights`).

### 변경

- **상단 `설정` 메뉴를 없앴다.** 눌러도 "준비 중"만 뜨던 유일한 버튼이었다.
  들어갈 내용이 캐릭터 설정뿐이라 속성 패널로 옮겼다.

### 참고 — 판정 기준을 두 번 고쳤다

처음에는 "경계가 또렷하고 색이 적으면 도트"로 봤는데, 플랫한 벡터풍 일러스트가
전부 도트로 판정됐다. 안티앨리어싱은 경계 한 줄에만 생겨 전체 비율로는 작지만,
**그 경계가 만든 어중간한 색은 몇 픽셀씩만 쓰인다**. 그래서 "몇 번 안 쓰인 색의 비율"을
지표로 더했더니 갈렸다 (도트 0% · 일러스트 82%).

### 검증

- `npm test` 130개 통과 (도트 판정 7개 · 격자 재표본 4개 추가)
- Chromium 확인: 4배 확대 도트 스프라이트 → 도트 + 격자 낮음 자동 적용 ·
  일반 일러스트 → 일반 유지 · 이름 변경 · 격자 보통 → 높음 → 낮음으로 바꿔도
  칠한 비율이 0.66 → 0.70 → 0.65로 유지됨 (보간 오차 범위)

---

## v0.11.0 — 2026-08-15

**Phase**: Phase 7 (Secondary Motion — 기획서 29) + 파츠 정리

### 추가

- **따라 흔들림(Secondary Motion).** `secondary` 태그가 붙은 관절이 몸을 한 박자 늦게
  따라 흔들린다. 애니메이션에 그런 Track이 없어도 런타임이 자동으로 만든다.
  - 관절마다 damped spring 하나만 돌린다. 물리 엔진은 쓰지 않는다. (기획서 29)
  - **회전 흔들림**: 부모의 가속도와 회전 속도를 입력으로 받아 좌우로 흔들리고 스프링이 되돌린다
  - **위치 지연**: 부모가 앞서 나간 만큼 뒤에 남았다가 따라붙는다.
    회전만으로는 위아래 움직임에 거의 반응하지 못해 함께 넣었다
  - 프레임이 튀어도 발산하지 않도록 시간 간격과 각도(0.6rad) · 뒤처짐(18px)에 상한을 뒀다
- **하단에 `흔들림` 막대**(0~2배)를 더했다. 애니메이션마다 저장된다 (Puppet JSON v6의 `secondary`).
- **늘어지는 파츠 10종.** 전부 `secondary` 태그가 기본으로 붙는다.
  `머리카락` `갈기` `수염` `망토` `옷자락` `지느러미` `더듬이` `리본` (기존 `꼬리` `촉수`와 함께)
- `턱`(jaw) · `방패`(shield) 파츠도 더했다. 태그는 있는데 만들 파츠가 없었다.

### 변경

- **`중심머리` 파츠를 없앴다.** `머리`와 `core` 태그 하나 차이뿐이라 구분이 서지 않았다.
  머리를 중심으로 삼고 싶으면 `머리`를 만든 뒤 태그 목록에서 `core`를 눌러 붙이면 된다.
- 파츠 목록을 몸 → 팔다리 → 늘어지는 부속 → 나머지 순으로 다시 정렬했다.

### 검증

- `npm test` 119개 통과 (흔들림 11개: 매달린 관절만 반응 · 첫 프레임 안 튐 · 멈추면 잦아듦 ·
  세기 0이면 무효 · 각도 상한 · 위치 지연 부호와 복귀)
- Chromium 확인: `머리카락` 관절이 `hair, secondary, light` 태그로 생성 ·
  점프 재생 중 최대 16.6px 뒤처지고 13도까지 흔들린 뒤 제자리로 복귀 ·
  `흔들림`을 0으로 두면 각도 0

---

## v0.10.0 — 2026-08-15

**Phase**: Phase 7 (애니메이션 파라미터 — 기획서 31)

### 추가

- **속도 · 강도 조절.** 하단 바에서 애니메이션을 고르면 오른쪽에 두 막대가 나타난다.
  - `속도` 0.1~3배 — 같은 프리셋으로 굼뜬 골렘과 잽싼 거미를 만든다
  - `강도` 0~2배 — 움직임 크기. 관절마다의 `강도` 값과 곱해진다
  - 막대에는 기본값 1 위치에 눈금이 있고, `되돌리기`로 둘 다 1로 되돌린다
- **값은 애니메이션에 저장된다.** 미리보기용 설정이 아니라 프로젝트 · 내보내기 · 게임까지
  그대로 따라간다. Puppet JSON v5의 `speed` · `strength`.
- **재생 중에 바꾸면 즉시 반영된다.** 멈췄다 다시 재생할 필요가 없다.

### 수정

- 막대를 끄는 동안 하단 줄이 다시 그려져 드래그가 끊기던 문제.
  같은 애니메이션이면 통째로 다시 그리지 않고 채움과 숫자만 갱신한다.
  (더블클릭 이름 바꾸기 때와 같은 원인이다)

### 검증

- `npm test` 108개 통과 (재생 중 속도 · 강도 변경, 강도와 motionStrength 곱 검사 추가),
  `npm run build` 성공
- Chromium 확인: 속도 0.4배로 낮추면 1초에 0.38초만 진행 · 강도 0.1이 재생 중 즉시 반영 ·
  `되돌리기`로 1/1 복귀 · 저장 파일에 값과 포맷 v5가 기록되는 것을 `unzip`으로 확인

---

## v0.9.3 — 2026-08-15

**Phase**: Phase 5 다듬기 (브러시 크기 단계)

### 변경

- **브러시 크기를 28단계 네 줄로 늘렸다.** 2px부터 500px까지 등비에 가깝게 나눈다.
  작은 쪽(2~10px)은 격자 한두 칸만 건드리는 미세 보정용, 큰 쪽(175px 이상)은
  몸통을 한 번에 덮는 용도다.
- **점 지름을 로그 눈금으로 매겼다.** 제곱근으로 매기니 작은 쪽 열 개가 4~6px에 뭉쳐
  구분이 되지 않았다. 이제 4px에서 26px까지 단계마다 고르게 벌어진다.
- 크기 버튼 높이를 26 → 30px로 키워 가장 큰 점이 들어가게 했다.

### 검증

- `npm test` 106개 통과, `npm run build` 성공
- Chromium 확인: 28단계가 정확히 네 줄 · 점 지름 4 → 26px로 단조 증가 ·
  가중치에 따른 진하기 반영 유지

---

## v0.9.2 — 2026-08-15

**Phase**: Phase 5 다듬기 (브러시 표현)

### 변경

- **브러시 크기를 14단계로 세분화하고 두 줄로 배치했다.**
  6 · 10 · 15 · 22 · 30 · 40 · 52 · 68 · 88 · 112 · 145 · 185 · 230 · 290px.
  한 줄에 일곱 개씩 격자로 놓아 패널 폭 안에 들어간다.
- **가중치가 브러시 표현에 반영된다.**
  - 캔버스 커서: 안쪽에서 바깥으로 옅어지는 고리 다섯 겹으로 falloff를 보여 주고,
    테두리 진하기도 가중치를 따른다. 가중치 5면 거의 비어 보이고 95면 또렷하게 찬다.
  - 크기 버튼의 점도 같이 옅어진다. 한 번 칠했을 때 얼마나 묻는지를 버튼에서 바로 읽는다.
    (가장 낮을 때도 크기는 구분돼야 하므로 진하기는 0.35 아래로 내리지 않는다)
  - 지우개일 때는 흰 계열로 그리고 안쪽 고리를 하나 더 둘러 칠하기와 구분한다.

### 검증

- `npm test` 106개 통과, `npm run build` 성공
- Chromium 확인: 14단계가 정확히 두 줄로 배치 · 점 지름 6~26px · 가중치 25 → 95로 올리면
  점 불투명도 0.41 → 0.96 · 가중치에 따라 캔버스 커서 픽셀이 실제로 달라짐

---

## v0.9.1 — 2026-08-15

**Phase**: Phase 5 다듬기 (브러시 조작)

### 변경

- **브러시 크기를 그림 도구처럼 점 버튼으로 고른다.** 슬라이더를 없애고 8단계
  (8 · 16 · 28 · 45 · 70 · 110 · 170 · 250px)를 실제 크기에 맞는 점으로 보여 준다.
  숫자를 읽지 않고 점 크기만 보고 고를 수 있다. 지름은 값의 제곱근에 비례시켜
  큰 값이 패널을 다 먹지 않게 했다.
- **가중치는 그래프로 조정한다.** 10칸 눈금이 그려진 막대를 누르거나 끌어서 정한다.
  눈금이 "몇 번 겹쳐 칠해야 가득 차는가"를 그대로 보여 준다.
- **순서를 바꿨다.** 가중치가 크기보다 위로 왔다. 한 번에 얼마나 쌓을지가 더 자주 바뀐다.
- 기본 브러시 크기를 40 → 45로 맞췄다. 선택지에 없는 값이라 아무 버튼도 켜지지 않았다.
  저장된 값이 선택지와 어긋나도 가장 가까운 버튼이 켜진다.

### 검증

- `npm test` 106개 통과, `npm run build` 성공
- Chromium 확인: 필드 순서(가중치 → 크기) · 점 지름 9~31px로 커짐 · 버튼 클릭으로 110px 적용 ·
  그래프 드래그로 60 적용 · 바뀐 값으로 실제 칠하기 동작

---

## v0.9.0 — 2026-08-15

**Phase**: Phase 7 (공격 계열 프리셋) + 자유 이름

### 추가

- **공격 계열 프리셋 6종.** 거미의 물기와 기사의 검질은 태그만으로는 갈라지지 않는다.
  "무엇이 움직이나"는 태그가, "어떻게 움직이나"는 프리셋이 정하도록 동작별로 나눴다.
  - `물기` — 머리째 달려들어 턱을 닫는다 (head · jaw · mouth)
  - `할퀴기` — 발톱으로 두 번 크게 긁어내린다 (attack · claw)
  - `휘두르기` — 크게 젖혔다가 호를 그리며 후려친다 (attack · weapon)
  - `찌르기` — 짧게 당겼다가 곧게 내지른다 (attack · weapon)
  - `몸통박치기` — 몸 전체로 부딪친다. 팔다리가 없는 슬라임도 된다 (core만 있으면 됨)
  - `캐스팅` — 팔을 들어 힘을 모았다 내린다 (arm · hand · weapon)
- **애니메이션 이름을 자유롭게 바꾼다.** 하단 이름을 더블클릭하면 그 자리에서 고칠 수 있다.
  이 이름이 곧 게임에서 부를 이름(`monster.play("attack")`)이다.
  거미엔 `물기`, 기사엔 `휘두르기`를 넣고 둘 다 `attack`으로 이름 지으면
  게임 코드는 몬스터 종류를 몰라도 된다.
  - 같은 프리셋을 두 번 넣으면 `bite`, `bite2`처럼 번호가 붙는다
  - 이미 쓰는 이름으로는 바꿀 수 없고, 그 이유를 알려 준다
- **`자유` 파츠.** 태그가 하나도 없는 맨 뼈대다. 추천 태그 없이 직접 골라 쓰고 싶을 때 쓴다.
- 프리셋 목록을 `기본` · `공격` 갈래로 나누고, 각 항목에 툴팁을 달았다
  (길이 · 반복 여부 · 트랙 수 · 쓰는 태그).

### 수정

- **하단 목록을 매번 다시 그리던 것을 고쳤다.** 재생할 때마다 버튼이 통째로 교체돼서
  브라우저가 더블클릭 이벤트를 아예 만들지 못했다(첫 클릭 대상이 사라지기 때문).
  목록 구성이 그대로면 재생 표시만 바꾸도록 해서 이름 바꾸기가 동작한다.

### 검증

- `npm test` 106개 통과, `npm run build` 성공
- Chromium 확인: `자유` 본이 태그 없이 생성 · 공격 갈래 7개와 툴팁 · 물기 두 번 추가 시
  `bite`/`bite2` · `bite` → `attack` 이름 변경 · 중복 이름 거부 ·
  내보낸 ZIP의 키와 `name` 필드가 모두 바뀐 이름으로 나가는 것 확인

---

## v0.8.1 — 2026-08-15

**Phase**: Phase 3 다듬기 (설명 툴팁)

### 변경

- **툴팁을 직접 만들어 쓴다.** 브라우저 기본 `title`은 뜨는 데 오래 걸리고 한 줄뿐이라,
  제목 · 설명 · 부연 세 줄짜리 툴팁 요소를 만들었다 (`editor/ui/tooltip.ts`).
  0.09초 만에 뜨고, 화면 밖으로 나가면 알아서 접힌다.
- **태그 툴팁에 "이 태그를 쓰는 동작"을 함께 보여 준다.** 프리셋을 실제로 훑어서 만들기 때문에
  프리셋이 늘면 자동으로 따라온다. 쓰는 동작이 없으면 없다고 알려 준다.
  - `root` → "이 태그를 쓰는 동작: 대기 · 이동 · 공격 · 포효 · 피격 · 사망 · 점프"
  - `jaw` → "이 태그를 쓰는 동작: 포효"
  - `eye` → "아직 이 태그를 쓰는 기본 동작은 없습니다"
- **변형을 드롭다운에서 버튼 네 개로 바꿨다.** `부드럽게` `형태 유지` `위치 고정` `완전 고정`을
  한눈에 비교할 수 있고, 각 버튼에 마우스를 올리면 무엇이 어떻게 되는지와 예시 파츠가 뜬다.
  (드롭다운은 펼치기 전에는 다른 선택지를 볼 수 없어 설명을 붙이기 어려웠다)
- 감이 잘 안 오던 태그 설명 20여 개를 예시까지 넣어 다시 썼다.
  예: `bounce` → "탄력 있게. 슬라임처럼 착지와 충격에서 통통 튀는 느낌을 줄 부위."

### 검증

- `npm test` 106개 통과, `npm run build` 성공
- Chromium 확인: 태그 칩 · 태그 목록 버튼 · 변형 버튼 세 곳 모두 툴팁 내용 확인,
  화면 밖으로 넘치지 않는 것과 클릭 시 값이 바뀌는 것까지 확인

---

## v0.8.0 — 2026-08-15

**Phase**: Phase 7 (프리셋 6종 추가) + 애니메이션 관리 · 내보내기

### 추가

- **프리셋 6종을 데이터로 추가했다.** 엔진 코드는 한 줄도 고치지 않았다.
  `이동` `공격` `포효` `피격` `사망` `점프` — 각각 `src/presets/*.json` 파일 하나다.
  - `포효`: 웅크렸다가 몸을 부풀리고(`core` scale) 고개를 젖히며(`head`) 턱을 벌린다(`jaw`).
    턱이 없는 캐릭터면 그 Track만 건너뛴다.
- **하단 애니메이션 바를 프로젝트 목록으로 바꿨다.** 고정 버튼이 아니라 이 캐릭터가 실제로
  가진 애니메이션만 보여 준다.
  - `+`: 프리셋 목록을 펼쳐 골라 넣는다 (이미 넣은 것은 비활성)
  - 이름: 재생. 재생 중인 칸은 테두리와 배경으로 표시
  - `−`: **숨김** — 프로젝트 파일에는 남기되 내보내기 결과에서만 뺀다. 다시 누르면 포함
  - `×`: 목록에서 삭제
- **`내보내기`가 동작한다.** 숨기지 않은 애니메이션만 담은 `<이름>.export.zip`을 만든다
  (puppet.json + 원본 이미지). `프로젝트 저장`은 숨긴 것까지 전부 담는다.
- Puppet JSON v4: 애니메이션에 `hidden` 추가.

### 변경

- **첫 관절은 `중심`으로 고정한다.** 관절이 하나도 없을 때는 파츠 선택이 `중심`으로 잠기고,
  `root` `core` `body` 태그가 함께 붙는다. 두 번째 관절부터는 자유롭게 고른다.
- 파츠 목록 맨 앞에 `중심`을 넣었다.
- 이미지를 처음 불러오면 `대기`가 자동으로 들어간다. 필요 없으면 `×`로 빼면 된다.

### 참고 — 창발적 애니메이션이 실제로 가능한가

`포효`를 시험 삼아 만들어 봤다. JSON 파일 하나(8개 Track)를 쓰고 목록에 한 줄 더한 것이
전부이며, 엔진 · 렌더러 · 편집기 코드는 건드리지 않았다. 태그만 맞으면 어떤 캐릭터에서도
돈다. (기획서 27, 56에서 노린 구조가 실제로 성립한다)

한계도 분명하다.
- 지금은 **프리셋을 고르는 것만** 되고, 편집기 안에서 키프레임을 만들거나 고칠 수는 없다.
  즉 "나만의 애니메이션"은 JSON을 직접 쓰거나 AI에게 시켜야 한다.
- 두 애니메이션을 섞는 **블렌딩은 없다**. 한 번에 하나만 재생한다.
- 속도 · 강도 슬라이더(기획서 31)도 아직 없다.

### 검증

- `npm test` 106개 통과 (프리셋 무결성 · 태그 사용 검사 · 내보내기 필터 포함), `npm run build` 성공
- Chromium 확인: 첫 관절이 `중심1 [root, core, body]`로 잠김 → 대기 자동 추가 →
  `+`로 포효 · 공격 추가 → 포효 재생 중 칠한 정점이 실제로 이동
  (x: -4 → -60 → -19) → 공격을 `−`로 숨김 → 내보내기 ZIP에는 `idle, roar`만,
  저장 ZIP에는 `attack(숨김)`까지 남는 것을 `unzip`으로 확인

---

## v0.7.0 — 2026-08-15

**Phase**: Phase 3 · 7 다듬기 (태그 체계)

### 추가

- **태그 목록을 41개로 늘렸다.** 7개 묶음으로 나눠 두었다.
  - 중심 · 구조: `root` `core` `body` `spine` `hip` `neck`
  - 팔다리: `arm` `hand` `leg` `foot` `claw` `finger`
  - 부속: `tail` `wing` `tentacle` `hair` `ear` `horn` `fin` `cloth` `antenna`
  - 얼굴: `head` `eye` `mouth` `jaw`
  - 역할: `attack` `weapon` `shield` `prop` `decoration` `ground`
  - 움직임 성격: `secondary` `float` `heavy` `light` `bounce` `stiff`
  - 위치 구분: `front` `back` `upper` `lower` (좌우는 여전히 강제하지 않는다 — 기획서 10)
- **태그마다 설명을 달았다.** 버튼과 칩에 마우스를 올리면 그 태그가 애니메이션에서
  무슨 일을 하는지 바로 뜬다.
- **태그 편집을 칩 + 버튼 방식으로 바꿨다.** 쉼표로 구분해 직접 적던 입력칸을 없앴다.
  - 붙어 있는 태그는 칩으로 보이고 `×`로 뗀다
  - `+ 태그`를 누르면 묶음별 목록이 펼쳐지고, 눌러서 켜고 끈다
  - 목록에 없는 태그는 `직접 입력`으로 만든다 (기획서 11)
- **변형 선택지에도 툴팁을 달았다.** 네 가지가 각각 어떤 파츠를 위한 것인지 설명한다.

### 수정

- **첫 루트 관절에 `root` 태그가 자동으로 붙는다.** 어떤 파츠에도 `root`가 배정되지 않아
  `대기` 프리셋의 "몸 전체 상하 흔들림" 트랙이 항상 빈 채로 지나가던 문제를 고쳤다.
  이미 `root`를 가진 관절이 있거나 부모가 있는 관절에는 붙이지 않는다.

### 변경

- 파츠별 추천 태그를 새 목록에 맞게 손봤다.
  뿔 → `horn, stiff`, 촉수 → `tentacle, secondary, attack`, 발 → `foot, ground` 등.

### 검증

- `npm test` 92개 통과 (태그 목록 무결성 · root 자동 배정 검사 포함), `npm run build` 성공
- Chromium 확인: 첫 관절에 `root, body, core` 자동 배정 · 칩 툴팁 · 묶음 7개와 버튼 41개 ·
  버튼으로 추가/제거 · 직접 입력 · 칩 `×` 제거 · 변형 툴팁까지 동작

---

## v0.6.0 — 2026-08-15

**Phase**: Phase 5 · 7 다듬기 (Bone 변형 방식 런타임 적용)

### 추가

- Bone 변형 방식을 움직임과 찌그러짐 여부의 조합 4가지로 확장했다.
  - `soft`: 가중치를 부드럽게 섞으며 위치 · 회전 · 크기를 적용한다.
  - `rigid`: 가장 강한 Rigid Bone 하나의 변환만 적용해 파츠 형태를 유지한다.
  - `pinnedSoft`: 자기와 부모의 움직임은 무시해 위치를 고정하고, 이웃 영향과의 경계는 부드럽게 휜다.
  - `fixed`: 다른 영향과 겹쳐도 원래 위치와 형태를 모두 고정한다.
- 네 모드 각각의 스킨 행렬과 정점 처리 결과를 검증하는 회귀 테스트를 추가했다.

### 수정

- 속성 패널에서 저장만 되던 `soft` / `rigid` 값이 실제 애니메이션 Mesh 변형에 반영되도록 했다.
- Idle처럼 루트가 위아래로 움직일 때 `pinnedSoft`와 `fixed` 발 관절이 부모를 따라 내려가던 동작을
  월드 기준 위치 고정으로 막았다.

### 변경

- Puppet JSON을 v3으로 올리고 기존 v1/v2 파일의 `soft` / `rigid` 의미를 그대로 호환한다.
- 속성 패널의 변형 선택 문구를 `부드럽게 · 움직임`, `형태 유지 · 움직임`,
  `부드럽게 · 위치 고정`, `형태·위치 모두 고정`으로 명확히 구분했다.

### 검증

- `npm test` 전체 통과
- `npm run build` 성공

### 참고

- `fixed` 가중치는 같은 정점의 다른 Bone 가중치보다 우선한다. 완전 고정 영역의 가장자리를
  부드럽게 이어야 한다면 `pinnedSoft`를 사용한다.

---

## v0.5.0 — 2026-08-15

**Phase**: Phase 5 다듬기 (영향 영역 표시)

### 추가

- **`전체 보기` 토글** (상단 우측, `영향 영역` 옆): 모든 관절의 영향 영역을 각자의 색으로 한 번에
  보여준다. 어느 관절도 가지지 않은 곳은 어둡게 덮이므로, 애니메이션에서 움직이지 않을 부분이
  한눈에 드러난다.
- 속성 패널에 `칠하지 않은 영역 N%` 표시. 빈 곳이 없으면 그렇다고 알려 준다.

### 변경

- **영향 영역 점을 훨씬 촘촘하게 깔았다.** 점을 Mesh 정점이 아니라 3px 간격으로 찍고 가중치를
  이중 선형 보간으로 읽는다. 격자 해상도와 무관하게 칠한 자리가 면처럼 보인다.
- 점 패턴을 캔버스 텍스처로 한 번 구워서 올린다. 매 프레임 수천 개를 그리지 않으므로
  점을 아무리 촘촘하게 깔아도 렌더링 비용이 늘지 않는다 (가중치가 바뀔 때만 다시 굽는다).
- 대비를 다시 잡았다. 고른 관절의 영역은 그 색으로 또렷하게, 나머지는 어둡게 눌러 둔다.
- 재생 중에는 점 패턴을 감춘다. 변형을 따라가지 않아 어긋나 보이기 때문이다.

### 참고

- 점 개수는 45,000개로 제한하고 큰 이미지에서는 간격을 자동으로 벌린다.
  칠하는 동안 매 프레임 다시 구울 수 있어야 하기 때문이다.

### 검증

- `npm test` 74개 통과, `npm run build` 성공
- Chromium 확인: 두 관절을 각각 칠한 뒤 기본 보기 · `전체 보기` · `영향 영역` 끄기까지
  화면으로 확인, 빈 영역 비율 표시 동작

---

## v0.4.0 — 2026-08-15

**Phase**: Phase 4 · 5 다듬기 (Mesh 밀도 · 클리핑 · 관절 색 · 도구 토글)

### 수정

- **`대기`를 눌러도 이미지가 전혀 움직이지 않던 문제.** Phaser의 `Mesh.preUpdate`는
  Mesh 자신의 변환이 그대로면 정점 투영을 통째로 건너뛴다. 우리는 정점 좌표를 직접 바꿔
  변형하므로 그 최적화에 걸려 화면에 반영되지 않았다. 변형 중에는 `ignoreDirtyCache`를 켜
  매 프레임 다시 계산하게 하고, 멈추면 한 번만 갱신한 뒤 다시 건너뛰게 했다.
  (이전 검증이 자체 계산 결과만 확인하고 실제 렌더링을 보지 않아 놓쳤다. 지금은 재생 중
  캔버스 픽셀이 실제로 바뀌는지 프레임 간 비교로 확인한다.)

### 추가

- **클리핑 마스크**: 이미지의 그려진 영역(알파 > 0) 안쪽 정점만 칠해지고 표시된다.
  투명 배경 PNG의 실루엣을 그대로 따라간다. 격자 반 칸만큼 여유를 둬 경계도 칠할 수 있다.
- **관절 색**: 관절마다 서로 다른 색이 자동으로 붙는다(황금각으로 색상환을 돌며 배정).
  관절점 · 연결선 · 영향 영역 표시 · 브러시 커서가 모두 이 색을 따르고, 좌측 목록에도 점으로 보인다.
- **원형 색 팔레트**: 속성 패널의 색 견본을 누르면 펼쳐진다. 각도가 색상, 중심에서의 거리가 채도다.
- Puppet JSON v2: Bone에 `color` 추가. v1 파일은 열 때 색을 자동으로 채운다.

### 변경

- 격자 밀도를 올렸다 (낮음 24 · 보통 48 · 높음 72). 영향 영역 점도 훨씬 작고 촘촘해졌다.
- 영향 영역 점을 원 대신 육각형으로 그린다. 정점이 수천 개라 매 프레임 원을 그리면 무겁다.
- `칠하기` · `지우개`를 아이콘 버튼 두 개로 묶어 섹션 맨 위에 두었다.
  한 번 누르면 켜지고, 같은 것을 다시 누르면 꺼지며, 다른 것을 누르면 그쪽으로 바뀐다.

### 참고

- 소프트웨어 GL(swiftshader) 환경에서 48칸 격자 기준 20~40fps로 측정된다. 오버레이 그리기는
  0.01ms로 무해하고 대부분 소프트웨어 렌더링 비용이라, 실제 GPU에서는 문제가 되지 않는다.
  느리면 Mesh 해상도를 낮추면 된다.

### 검증

- `npm test` 74개 통과, `npm run build` 성공
- Chromium 확인: 마스크 안쪽 1222개 정점만 칠해짐(바깥 0개) · 도구 토글 4단계 전환 ·
  팔레트로 색 변경 · **재생 중 캔버스 픽셀이 프레임마다 실제로 변함** · 저장 후 재불러오기 복원

### 다음

Phase 7 나머지 프리셋(Attack · Hit · Death · Jump), 애니메이션 파라미터 슬라이더,
IndexedDB 자동 저장.

---

## v0.3.0 — 2026-08-15

**Phase**: Phase 4 · 5 · 6 · 8 (Mesh · Weight · Animation Runtime · 저장/불러오기)

### 추가

- **Mesh** (`core/mesh`): 이미지 크기에 맞춰 격자 Mesh 자동 생성. 셀이 정사각형에 가깝게
  긴 변 기준으로 나눈다. 해상도 낮음 16 · 보통 32 · 높음 64.
- **Weight** (`core/weight`): 원 · 캡슐 영향 영역, Soft Falloff, 정점당 최대 4개 Bone,
  겹친 영역 자동 정규화. 브러시는 수채화처럼 쌓인다 — `가중치` 10이면 열 번 칠해야 가득 차고
  100이면 한 번에 최대. 지우개는 같은 크기 · 같은 양으로 깎는다.
- **스킨 변형** (`core/skeleton/transform`): 2D 아핀 행렬, 부모 → 자식 계층 변환,
  Linear Blend Skinning. 칠하지 않은 정점은 움직이지 않는다.
- **Animation Runtime** (`core/animation`): 키프레임 보간(linear · step · smooth),
  태그 기반 Track 대상 찾기, motionStrength 반영, 루프, 이벤트, `AnimationPlayer`.
  요구한 태그가 없으면 그 Track만 건너뛴다.
- **Idle 프리셋** (`src/presets/idle.json`): root 상하, core squash, head 회전,
  secondary 지연 흔들림. 코드가 아니라 데이터다.
- **프로젝트 저장 / 불러오기**: `puppet.json` + 원본 이미지를 ZIP 하나로 묶는다
  (`character.puppet.zip`). 의존성 없이 직접 구현했고, 쓰기는 무압축 · 읽기는 deflate도 지원한다.
  `Ctrl+S`로도 저장된다. ZIP을 캔버스에 끌어다 놓아도 열린다.
- 우측 속성 패널에 `영향 영역` 섹션: `칠하기` 토글, 크기 · 가중치 슬라이더, `지우개`
- 캔버스에 브러시 원과 정점별 가중치 표시(밝을수록 강함)

### 변경

- **상단 우측 편집 모드 탭을 표시 토글로 바꿨다.** `관절` / `연결` / `영향 영역` 3개이며
  기본은 모두 켜짐, 끄면 캔버스에서 해당 표시만 사라진다. `애니메이션` · `미리보기`는
  하단 바와 중복이라 제거했다. 감춘 관절은 집을 수도 없다.
- 이미지를 불러오면 Mesh가 자동으로 만들어지고, 캔버스는 Sprite 대신 변형 가능한 Mesh를 그린다.
  (WebGL이 없으면 원본 이미지를 그대로 표시)
- Undo / Redo가 가중치 편집 상태까지 함께 되돌린다.

### 참고

- **관절 생성 방식은 `추가` 버튼 + 드래그로 확정했다.** 캔버스 클릭 생성과 별도 `연결` 모드는
  만들지 않는다. 이 결정은 `CLAUDE.md`의 "확정된 제품 결정"에 적어 두었다.
- Puppet JSON 포맷(v1)은 그대로다. Mesh · Weight · Animation 필드를 실제로 채우기 시작했다.

### 검증

- `npm test` 66개 통과 (`mesh` 6 · `weight` 15 · `transform` 8 · `animation` 15 · `zip` 7 외),
  `npm run build` 성공
- ZIP은 표준 `unzip -t`로 검사해 무결성을 확인한다 (테스트에 포함)
- Chromium 전 과정 확인: 이미지 → Mesh 25×32 생성 → 관절 추가 → 캔버스에 칠하기(858개 중 194개)
  → `대기` 재생으로 정점이 실제로 움직임 → 저장 → 새로고침 후 불러오기까지 가중치 · 텍스처 복원

### 다음

Phase 7 나머지 프리셋(Attack · Hit · Death · Jump), 애니메이션 파라미터 슬라이더,
IndexedDB 자동 저장, Phase 9 · 10 (runtime-core · runtime-phaser 분리).

---

## v0.2.0 — 2026-08-15

**Phase**: Phase 1 마무리 + Phase 3 착수 (관절 편집)

### 추가

- 좌측 관절 목록에서 드래그로 순서 변경 (`⠿` 그립, 드롭 위치 표시선)
- 관절 목록 각 줄 우측에 `×` 삭제 버튼
- 캔버스에서 관절점을 직접 클릭해 선택하고 끌어서 위치 수정
  - 관절 위에서 커서가 `grab`으로 바뀜
  - 빈 곳 클릭 시 선택 해제
  - 드래그 한 번이 Undo 한 단위
- 새 관절은 이미지 중앙이 아니라 **현재 보고 있는 화면 중앙**에 생성되며, 연달아 추가하면 조금씩 어긋나게 놓인다
- `CLAUDE.md` 작업 지침, `VERSION.md` 버전 기록

### 변경

- UI 전체를 어두운 그레이 + 흑백 톤으로 정리
  - 파란 강조색 제거. 선택 상태는 흰색 계열과 명도 차로만 표현
  - 배경 4단계(`--bg-canvas` → `--bg-control-hover`), 글자 3단계로 위계 정리
  - 편집 모드 탭은 선택 시 흑백 반전
  - 타이포그래피 통일: Pretendard 우선 스택, 패널 제목 11px 대문자 트래킹, 숫자 입력 tabular-nums
  - 버튼은 기본 무테 · hover 시 배경, 스크롤바도 같은 톤으로
- 캔버스 관절점을 흰 점 + 어두운 테두리로 변경. 선택된 관절만 링과 중심점 표시
- Mesh 격자 가이드와 캔버스 배경을 더 어둡고 옅게
- 속성 패널의 `관절 삭제` 버튼은 목록의 `×`로 옮기고, 대신 현재 좌표를 표시

### 검증

- `npm test` 16개 통과, `npm run build` 성공
- Chromium에서 확인: 관절 3개 추가 → 캔버스 드래그로 이동(153, 83) → 빈 곳 클릭 선택 해제 → 목록 드래그로 순서 변경 → `×` 삭제까지 정상, 콘솔 오류 없음

### 다음

Phase 3 나머지: 캔버스에서 클릭으로 관절 생성, 연결 모드에서 부모 지정.

---

## v0.1.0 — 2026-08-15

**Phase**: Phase 1 (프로젝트 초기화)

### 추가

- Vite + TypeScript + Phaser 프로젝트 설정, Vitest, Vercel 배포 설정
- 기획서 61 기준 폴더 구조 (`core` / `editor` / `renderer`)
- `core/format`: Puppet JSON 타입 정의, 파츠 · 태그 상수, 생성 · 검증 · 직렬화, 이름 자동 번호
- `core/skeleton`: 태그 조회, 계층 정렬, 부모 변경 순환 검사
- 한국어 편집기 레이아웃 (상단 메뉴 / 관절 · 속성 패널 / 편집 모드 탭 / 애니메이션 바)
- PNG · WebP 드래그 앤 드롭, 휠 Zoom, 우클릭 · 휠클릭 Pan
- 관절 추가 · 삭제와 이름 · 부모 · 태그 · 강도 · 변형 편집, 관절점과 부모 연결선 표시
- Undo / Redo (`Ctrl+Z`, `Ctrl+Y`, `Ctrl+Shift+Z`)
- 코어 로직 테스트 16개, `docs/puppet-json.md`, 한국어 README

### 수정

- Phaser 카메라 확대/축소가 커서 위치를 기준으로 동작하지 않던 문제
  (`getWorldPoint`는 preRender에서 갱신되는 행렬을 쓰므로 `setZoom` 직후 값이 낡는다.
  화면 중심 오프셋으로 직접 보정)

### 참고

- Puppet JSON `version`: 1
- 미구현: Mesh, Weight, 애니메이션 재생, 프로젝트 저장 · 불러오기, runtime 패키지
