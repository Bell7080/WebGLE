# 범용 웹 종이인형 애니메이션 툴 개발 기획서

## 0. 프로젝트 정의

브라우저에서 실행되는 개인용 2D 종이인형 애니메이션 제작 툴을 개발한다.

이 툴의 목적은 AI 일러스트, 투명 PNG 캐릭터, 도트 캐릭터 등의 **한 장짜리 캐릭터 이미지**에 사용자가 직접 관절점을 지정하고 영향 영역을 칠한 뒤, 범용 애니메이션을 적용하여 게임에서 사용할 수 있는 가벼운 애니메이션 데이터를 생성하는 것이다.

Spine의 완전한 대체품을 만드는 것이 목적이 아니다.

다음에 집중한다.

- 설치 없이 브라우저 실행
- 한국어 UI
- 매우 단순한 작업 흐름
- 임의 구조의 몬스터 지원
- 한 장 PNG 기반
- 가벼운 Mesh 변형
- 범용 애니메이션 프리셋
- Phaser / HTML / TypeScript에서 즉시 사용
- 여러 게임에서 반복 사용
- 수십 종 이상의 몬스터 동시 운용 가능
- 향후 Unity / Unreal 런타임 추가 가능
- AI 코딩 에이전트가 기능과 애니메이션을 쉽게 추가할 수 있는 구조

프로젝트 임시명:

**PuppetForge**

명칭은 추후 변경 가능하다.

---

# 1. 가장 중요한 개발 원칙

## 1.1 스켈레톤 타입을 만들지 않는다

다음과 같은 분류를 엔진에서 사용하지 않는다.

- 인간형
- 4족보행
- 비행형
- 뱀형
- 곤충형

모든 캐릭터는 단순한 **노드와 부모 관계의 집합**이다.

예:

```text
루트
 └ 몸통1
    ├ 머리1
    ├ 머리2
    ├ 오른팔1
    ├ 꼬리1
    │  └ 꼬리2
    │     └ 꼬리3
    └ 다리1
```

다른 캐릭터:

```text
루트
 └ 몸통1
    ├ 중심머리1
    ├ 머리1
    ├ 머리2
    ├ 머리3
    ├ 오른팔1
    └ 오른팔2
```

엔진은 이 둘의 생물학적 차이를 알 필요가 없다.

---

# 2. 핵심 사용자 경험

전체 제작 절차는 최대한 다음처럼 단순해야 한다.

```text
이미지 넣기

→ 관절 추가

→ 관절 위치 찍기

→ 관절 연결

→ 영향 영역 칠하기

→ 기본 애니메이션 적용

→ 미리보기

→ 저장 / 내보내기
```

목표:

**익숙해진 사용자가 캐릭터 하나를 세팅하는 데 수 분 이상 걸리지 않는 구조.**

복잡한 전문 애니메이션 프로그램처럼 만들지 않는다.

---

# 3. 실행 환경

## 필수

- 웹 브라우저
- Chromium 계열 브라우저 우선
- WebGL 지원
- 설치 불필요
- 서버 설치 불필요
- 로그인 불필요한 기본 모드 제공

지원 목표:

- Chrome
- Edge
- Chromium 기반 제한 환경

저사양 PC에서도 작동해야 한다.

학교, 공용 PC 등에서 URL만 접속하여 사용할 수 있어야 한다.

---

# 4. 기술 스택

우선 다음 기술을 사용한다.

```text
TypeScript
Vite
HTML
CSS
Phaser
WebGL
Canvas API
File API
```

React는 반드시 필요하지 않다.

UI 복잡도가 커지기 전에는 가능한 한 의존성을 줄인다.

권장 구조:

```text
Vite
+ TypeScript
+ Phaser
+ Vanilla HTML/CSS
```

에디터 UI 자체는 DOM으로 만든다.

캐릭터 렌더링 영역만 Phaser/WebGL을 사용한다.

---

# 5. 배포

저장소:

```text
GitHub
```

배포:

```text
GitHub
 ↓
Vercel
 ↓
브라우저 접속
```

GitHub 저장소의 main 브랜치가 업데이트되면 자동 배포할 수 있도록 구성한다.

가능하면 정적 사이트 형태를 유지한다.

초기 버전에서는 데이터베이스나 서버를 사용하지 않는다.

---

# 6. 프로젝트 저장 방식

프로젝트 하나는 다음 구조를 가진다.

```text
project/
 ├ character.png
 ├ character.puppet.json
 └ animations/
```

실제로 브라우저에서는 ZIP 하나로 저장해도 된다.

예:

```text
UndeadWolf.puppet.zip
```

내용:

```text
character.png
puppet.json
```

애니메이션 프리셋은 툴 자체에 내장할 수 있다.

---

# 7. Puppet JSON

프로젝트의 핵심은 엔진 독립적인 JSON 포맷이다.

예:

```json
{
  "format": "puppetforge",
  "version": 1,

  "character": {
    "name": "언데드 늑대",
    "texture": "character.png",
    "width": 512,
    "height": 512
  },

  "bones": [],

  "mesh": {},

  "animations": {}
}
```

이 JSON을 PuppetForge의 단일 소스로 취급한다.

Phaser용 파일, Unity용 파일 등을 따로 원본으로 사용하지 않는다.

---

# 8. 관절 / Bone 시스템

Bone은 다음 정보를 가진다.

```ts
interface PuppetBone {
  id: string;
  name: string;

  parentId: string | null;

  x: number;
  y: number;

  rotation: number;

  scaleX: number;
  scaleY: number;

  tags: string[];

  motionStrength: number;
}
```

---

# 9. 파츠 이름 시스템

사용자가 GUI에서 원하는 파츠를 추가한다.

기본 선택지:

```text
머리
중심머리
몸통
팔
다리
손
발
꼬리
날개
목
뿔
귀
눈
입
촉수
집게
무기
장식
기타
```

같은 파츠를 여러 개 추가할 수 있다.

자동 이름:

```text
머리1
머리2
머리3

팔1
팔2
팔3

꼬리1
꼬리2
꼬리3
```

사용자가 이름을 수정할 수도 있다.

---

# 10. 좌우 개념을 강제하지 않는다

다음 구조를 강제하지 않는다.

```text
왼팔
오른팔
왼다리
오른다리
```

필요하면 사용자가 직접 그렇게 이름을 붙인다.

예:

```text
오른팔
큰팔
집게팔
등뒤팔
```

엔진에서는 모두 임의의 Bone이다.

---

# 11. Tag 시스템

Bone에는 여러 개의 태그를 붙일 수 있다.

예:

```text
머리1

head
attack
secondary
```

예:

```text
사마귀낫1

arm
weapon
attack
```

예:

```text
꼬리3

tail
attack
secondary
```

기본 태그:

```text
root
body
core

head
neck

arm
hand

leg
foot

tail

wing

weapon

attack

secondary
```

사용자가 직접 태그를 추가할 수도 있다.

---

# 12. Tag의 목적

범용 애니메이션이 정확한 Bone 이름에 의존하지 않도록 한다.

예:

```text
Idle
```

애니메이션이 다음 태그를 찾는다.

```text
core
head
secondary
```

캐릭터에 `head`가 없으면 해당 동작은 무시한다.

head가 3개면 세 개 모두 움직인다.

---

# 13. 부모 관계

사용자는 Bone끼리 연결할 수 있다.

예:

```text
몸통1
↓
목1
↓
머리1
```

또는:

```text
몸통1
↓
꼬리1
↓
꼬리2
↓
꼬리3
```

부모가 움직이면 자식도 따라간다.

---

# 14. 편집기 조작 방식

## 관절 모드

화면에서 클릭:

```text
관절 생성
```

드래그:

```text
관절 이동
```

관절 A에서 관절 B로 연결:

```text
A = 부모
B = 자식
```

UI에서 부모를 직접 선택하는 방식도 지원한다.

---

# 15. Mesh 구조

원본 PNG를 작은 2D Mesh로 변환한다.

예:

```text
32 x 32 grid
```

또는 이미지 크기에 따라 자동 조절한다.

Mesh 해상도 옵션:

```text
낮음
보통
높음
```

기본값:

```text
보통
```

목표는 고품질 Live2D가 아니다.

캐릭터가 자연스럽게 휘고 찌그러지는 정도면 충분하다.

---

# 16. Weight 시스템

전문적인 Spine식 Weight Painting UI를 만들지 않는다.

사용자는 Bone을 선택하고 캐릭터 위를 브러시로 칠한다.

UI:

```text
영향 영역

브러시 크기
[────●────]

강도
[──────●──]

부드러움
[────●────]

[지우기]
```

브러시 중심:

```text
weight = 높음
```

가장자리:

```text
weight = 낮음
```

Soft Falloff를 사용한다.

---

# 17. 자동 블렌딩

여러 Bone의 영향 영역이 겹치면 자동으로 Weight를 정규화한다.

예:

```text
어깨

몸통: 0.35
팔:   0.65
```

사용자가 숫자를 직접 관리할 필요는 없다.

---

# 18. 빠른 Weight 지정

추가 기능:

Bone을 선택한 뒤 드래그하여 원형 또는 캡슐 형태의 영향 영역을 만들 수 있다.

예:

```text
머리 → 원
팔 → 긴 캡슐
꼬리 → 긴 캡슐
```

그 영역 안에 자동 Weight를 생성한다.

이를 기본 기능으로 한다.

브러시 Weight Painting은 세부 보정용이다.

---

# 19. Rigid / Soft

Bone마다 두 가지 변형 방식 제공.

## Soft

Mesh deformation 사용.

```text
팔
몸통
꼬리
얼굴
```

등.

## Rigid

이미지 영역을 찌그러뜨리지 않는다.

위치 / 회전 / 크기만 적용한다.

용도:

```text
검
왕관
안경
장식
방패
```

---

# 20. 애니메이션 데이터

애니메이션 역시 JSON 기반으로 저장한다.

예:

```json
{
  "name": "idle",
  "duration": 1.5,
  "loop": true,

  "tracks": []
}
```

각 Track은 Bone ID 또는 Tag를 대상으로 할 수 있다.

---

# 21. 범용 애니메이션

초기 기본 애니메이션:

```text
대기
이동
공격
몸통박치기
할퀴기
물기
때리기
피격
사망
점프
착지
캐스팅
```

전부 사용할 필요는 없다.

캐릭터에서 원하는 것만 선택한다.

---

# 22. 기본 대기 모션

대기 모션은 가능한 한 모든 캐릭터에서 작동해야 한다.

예:

```text
root
→ 미세한 상하 움직임

core
→ 작은 scale 변화

head[]
→ 약간의 회전

secondary[]
→ 지연된 흔들림
```

머리가 없으면 head 부분을 생략한다.

---

# 23. 피격 모션

```text
root
→ 뒤로 짧게 이동

core
→ 짧게 squash

head[]
→ 반동

secondary[]
→ 관성 반동
```

---

# 24. 사망 모션

```text
root
→ 아래로 이동

core
→ 회전

head[]
→ 조금 늦게 따라옴

secondary[]
→ 처짐
```

---

# 25. 점프 모션

점프는 특정 체형을 전제로 하지 않는다.

```text
root.y 이동

출발:
scaleY 약간 증가

공중:
원래 크기

착지:
scaleY 감소
scaleX 증가

복귀
```

---

# 26. 애니메이션 프리셋 구조

애니메이션 프리셋은 별도 파일로 관리한다.

```text
src/presets/

idle.json
hit.json
death.json
jump.json
body-slam.json
scratch.json
bite.json
cast.json
```

새로운 모션을 추가할 때 엔진 코드를 수정하지 않는 것을 목표로 한다.

---

# 27. AI 친화적인 애니메이션 구조

Codex 또는 다른 코딩 AI에게 다음처럼 요청할 수 있어야 한다.

```text
"attack 태그가 붙은 Bone을 앞으로 내밀었다가
빠르게 되돌리는 찌르기 애니메이션을 추가해."
```

가능하면 AI는:

```text
animations/stab.json
```

파일만 추가하면 된다.

---

# 28. Motion Strength

각 Bone은 다음 값을 가진다.

```text
motionStrength
```

기본:

```text
1.0
```

예:

```text
몸통 = 0.5
머리 = 0.8
꼬리 = 1.5
촉수 = 2.0
무기 = 0.3
```

범용 애니메이션의 움직임 크기에 곱한다.

---

# 29. Secondary Motion

꼬리, 머리카락, 촉수 등의 움직임을 자동화한다.

태그:

```text
secondary
```

부모 Bone의 위치/회전을 약간 늦게 따라간다.

필요 값:

```text
delay
strength
damping
```

초기 구현에서는 정밀 물리 엔진을 사용하지 않는다.

간단한 spring / lerp 기반 구현으로 충분하다.

---

# 30. 애니메이션 UI

하단에 간단한 애니메이션 패널 제공.

```text
[대기]
[이동]
[공격]
[피격]
[사망]
[점프]
```

클릭하면 바로 재생한다.

---

# 31. 속성 조절

선택 애니메이션에 다음 값을 제공한다.

```text
속도
강도
움직임 크기
Squash
Secondary Motion
```

모두 Slider 형태.

---

# 32. Timeline

초기 버전에서는 전문 Timeline Editor를 만들지 않는다.

최소 기능만 제공한다.

```text
▶
■

현재 시간
애니메이션 길이
재생 속도
```

추후 필요하면 Keyframe 편집 기능을 추가한다.

---

# 33. 사용자 인터페이스

전체 UI는 반드시 한국어다.

예상 화면:

```text
┌────────────────────────────────────────────┐
│ 새 프로젝트  열기  저장  내보내기          │
├──────────┬─────────────────────┬───────────┤
│          │                     │           │
│ 관절     │                     │ 속성      │
│          │                     │           │
│ 몸통1    │      캐릭터         │ 이름      │
│ 머리1    │                     │ 부모      │
│ 팔1      │                     │ 태그      │
│ 꼬리1    │                     │ 강도      │
│ 꼬리2    │                     │           │
│          │                     │           │
│ + 추가   │                     │           │
├──────────┴─────────────────────┴───────────┤
│ 대기 이동 공격 피격 사망 점프   ▶          │
└────────────────────────────────────────────┘
```

---

# 34. 상단 메뉴

```text
새 프로젝트

이미지 불러오기

프로젝트 열기

프로젝트 저장

내보내기

설정
```

---

# 35. 편집 모드

화면 상단 또는 좌측에 다음 모드 제공.

```text
[관절]

[연결]

[영향 영역]

[애니메이션]

[미리보기]
```

---

# 36. Undo / Redo

필수.

```text
Ctrl + Z
Ctrl + Y
```

지원.

최소 다음 작업은 Undo 가능해야 한다.

```text
Bone 생성
Bone 삭제
Bone 이동
부모 변경
Weight 수정
Tag 변경
```

---

# 37. 로컬 저장

브라우저 IndexedDB에 자동 저장한다.

자동 저장:

```text
30초마다
```

또는 변경 시 debounce 저장.

브라우저가 종료되더라도 최근 프로젝트를 복구할 수 있게 한다.

---

# 38. 프로젝트 파일 저장

사용자가:

```text
프로젝트 저장
```

을 누르면:

```text
character.puppet
```

또는

```text
character.puppet.zip
```

다운로드.

다시 업로드하면 완전히 복원된다.

---

# 39. Runtime 구조

편집기와 실제 게임용 Runtime을 분리한다.

```text
packages/

editor/
runtime-core/
runtime-phaser/
```

게임에는 Editor 코드가 들어가지 않아야 한다.

---

# 40. runtime-core

다음만 담당한다.

```text
JSON Parsing

Skeleton

Bone Transform

Animation

Interpolation

Secondary Motion

Weight 계산
```

렌더링 엔진에 의존하지 않도록 한다.

---

# 41. runtime-phaser

Phaser와 연결한다.

예상 사용법:

```ts
const creature = await PuppetCreature.load(
  scene,
  "undead-wolf",
  "/monsters/undead-wolf/"
);

creature.play("idle");
```

전환:

```ts
creature.play("attack");
```

---

# 42. 게임 이벤트

애니메이션에는 Event를 넣을 수 있다.

예:

```json
{
  "time": 0.32,
  "event": "impact"
}
```

Phaser 게임에서:

```ts
creature.on("impact", () => {
  enemy.takeDamage();
});
```

활용:

```text
공격 판정
효과음
카메라 흔들림
이펙트
투사체 생성
```

---

# 43. Phaser Runtime 목표

게임에서 다음처럼 사용할 수 있어야 한다.

```ts
monster.play("idle");

monster.play("attack");

monster.play("hit");

monster.play("death");
```

그리고:

```ts
monster.setFlipX(true);

monster.setScale(2);

monster.setAnimationSpeed(1.2);
```

정도의 간단한 API 제공.

---

# 44. HTML / TypeScript 독립 Runtime

Phaser가 없는 웹 프로젝트에서도 사용할 수 있도록 장기적으로 다음 Runtime을 고려한다.

```text
runtime-webgl
```

그러나 V1에서는 Phaser Runtime을 우선한다.

---

# 45. Unity 지원

V1 필수 기능이 아니다.

Puppet JSON 구조는 Unity에서도 파싱 가능한 일반 JSON으로 유지한다.

추후:

```text
PuppetForge Unity Runtime
```

을 만든다.

대략:

```text
Puppet JSON
↓
C# 데이터
↓
Mesh
↓
Bone Transform
```

구조.

---

# 46. Unreal 지원

V1 필수 기능이 아니다.

동일한 JSON 포맷을 유지한다.

추후:

```text
PuppetForge Unreal Runtime
```

에서 JSON 데이터를 읽고 Dynamic Mesh 또는 적절한 2D Renderer로 표현한다.

---

# 47. Sprite Sheet Export

선택 기능.

사용자가:

```text
스프라이트 시트로 내보내기
```

선택.

설정:

```text
FPS
크기
애니메이션
열 수
여백
```

브라우저에서 프레임을 렌더링하여 PNG Sprite Sheet 생성.

---

# 48. Sprite Sheet Export 목적

Puppet Runtime을 사용하기 어려운 엔진에서도 사용할 수 있도록 한다.

예:

```text
GameMaker
RPG Maker
기타 자체 엔진
```

---

# 49. 성능 원칙

수십 종의 몬스터를 게임에서 사용할 수 있어야 한다.

그러나 동시에 수십 개의 복잡한 Mesh를 매 프레임 변형하는 것은 제한한다.

권장:

```text
캐릭터당 Bone

대략 5~30개
```

권장 Mesh:

```text
16x16
32x32
```

고해상도:

```text
64x64
```

는 선택.

---

# 50. LOD

장기적으로 간단한 LOD 제공 가능.

멀리 있는 캐릭터:

```text
Mesh 변형 빈도 감소
```

예:

```text
화면 중심:
60 FPS update

멀리:
30 FPS

매우 멀리:
15 FPS
```

렌더링 자체의 FPS가 아니라 Skeleton 업데이트 빈도만 조절한다.

---

# 51. Pixel Art 모드

도트 캐릭터용 옵션.

```text
[도트 모드]
```

활성화 시:

```text
Nearest Neighbor

Texture smoothing OFF

Mesh 해상도 낮춤

과한 deformation 제한
```

회전/찌그러짐은 허용한다.

완전히 깨끗한 픽셀 회전을 목표로 하지 않는다.

---

# 52. 일반 일러스트 모드

기본.

```text
Texture smoothing ON
Soft deformation
```

---

# 53. 이미지 요구사항

권장:

```text
PNG
투명 배경
```

지원:

```text
PNG
WebP
```

V1에서는 JPG 제외 가능.

---

# 54. 이미지 자동 배경 제거

구현하지 않는다.

사용자가 이미 배경 제거된 이미지를 넣는 것을 기본으로 한다.

---

# 55. AI 이미지 분석

V1에서 구현하지 않는다.

다음 기능은 필요 없다.

```text
자동 관절 인식
자동 팔 찾기
자동 머리 인식
AI segmentation
AI pose estimation
```

사용자가 직접 점을 찍는 것으로 충분하다.

---

# 56. AI의 역할

AI는 런타임 기능 확장과 Animation Preset 제작에 활용한다.

예:

```text
"몸을 크게 뒤로 젖혔다가 돌진하는 공격 추가"

"죽을 때 머리가 먼저 떨어지는 모션 추가"

"secondary Bone이 물속처럼 느리게 움직이는 옵션 추가"

"공중부양 idle 추가"
```

대부분 JSON 또는 작은 코드 변경으로 해결할 수 있게 설계한다.

---

# 57. 하지 않을 것

V1에서는 다음을 만들지 않는다.

```text
전문 Live2D 기능

Spine 완전 호환

복잡한 IK Editor

정교한 Physics Editor

Face Tracking

Motion Capture

영상 편집

수백 개 Keyframe UI

AI 자동 Rigging

서버 렌더링

클라우드 프로젝트 시스템

협업 시스템

회원 시스템

마켓플레이스
```

---

# 58. MVP 범위

우선 여기까지만 완성한다.

## Editor

- PNG 업로드
- 화면 표시
- Bone 생성
- Bone 삭제
- Bone 이동
- 이름 설정
- 부모 설정
- Tag 설정
- Bone 계층 표시
- 영향 영역 생성
- Soft Weight
- Weight 브러시
- Mesh 미리보기
- 프로젝트 저장
- 프로젝트 불러오기
- Undo / Redo

## Animation

- Idle
- Attack
- Hit
- Death
- Jump

## Runtime

- Core Runtime
- Phaser Runtime
- Animation Playback
- Loop
- Event
- Flip
- Scale

## Export

- Puppet JSON
- 프로젝트 ZIP

---

# 59. 2단계

MVP 이후:

```text
Sprite Sheet Export

Pixel Mode

Secondary Motion

애니메이션 파라미터

Body Slam

Scratch

Bite

Cast

Walk

Animation Event Editor
```

---

# 60. 3단계

필요한 경우:

```text
Unity Runtime

Unreal Runtime

Mesh 자동 최적화

Animation Blend

Attachment

Skin

장비 교체

표정 파츠

이미지 파츠 교체
```

---

# 61. 폴더 구조

권장:

```text
puppet-forge/

├ public/
│
├ src/
│
│  ├ editor/
│  │  ├ ui/
│  │  ├ tools/
│  │  ├ state/
│  │  └ history/
│  │
│  ├ core/
│  │  ├ skeleton/
│  │  ├ mesh/
│  │  ├ weight/
│  │  ├ animation/
│  │  ├ physics/
│  │  └ format/
│  │
│  ├ renderer/
│  │  └ phaser/
│  │
│  ├ presets/
│  │  ├ idle.json
│  │  ├ attack.json
│  │  ├ hit.json
│  │  ├ death.json
│  │  └ jump.json
│  │
│  └ export/
│
├ packages/
│  ├ runtime-core/
│  └ runtime-phaser/
│
├ examples/
│  └ phaser-demo/
│
├ tests/
│
├ index.html
├ package.json
├ tsconfig.json
└ vite.config.ts
```

---

# 62. 코드 설계 원칙

강하게 지킬 것.

```text
Editor와 Runtime 분리

Renderer와 Animation 로직 분리

JSON Format과 UI 분리

Animation Preset 데이터화

특정 캐릭터 구조 하드코딩 금지

특정 Bone 이름 의존 금지

Tag 기반 범용 처리

모든 public interface TypeScript 타입 정의
```

---

# 63. 하드코딩 금지 예

금지:

```ts
if (character.type === "human") {
}
```

금지:

```ts
const leftArm = bones["leftArm"];
```

허용:

```ts
const attackers = skeleton.getBonesByTag("attack");
```

---

# 64. 예외 처리

캐릭터에 Animation Preset에서 요구하는 태그가 없어도 오류가 나면 안 된다.

예:

```text
scratch animation

attack Bone 없음
```

결과:

```text
해당 Track Skip
```

전체 애니메이션은 계속 재생.

---

# 65. 데이터 버전

Puppet JSON에는 반드시:

```json
{
  "version": 1
}
```

포함.

추후 Format 변경 시 Migration 가능하도록 한다.

---

# 66. 테스트 캐릭터

최소 다음 비정형 캐릭터로 테스트한다.

```text
인간형

팔 하나 몬스터

머리 세 개 몬스터

꼬리 긴 몬스터

촉수 여러 개 몬스터

날개 몬스터

다리가 없는 몬스터
```

이 테스트를 통과해야 임의 Skeleton이라는 목적이 성립한다.

---

# 67. 테스트 목표

각 캐릭터에서 최소:

```text
Idle
Attack
Hit
Death
Jump
```

가 오류 없이 실행되어야 한다.

없는 파츠는 자동 Skip되어야 한다.

---

# 68. 성능 테스트

테스트 Scene에 동일 또는 서로 다른 Puppet을:

```text
1
10
25
50
```

개 배치.

FPS와 CPU 사용량 확인.

성능에 문제가 있으면 다음 순서로 최적화한다.

```text
Mesh subdivision 감소

Bone matrix caching

Weight precomputation

Animation Track caching

offscreen update 감소

Secondary Motion update 감소
```

---

# 69. 완료 기준

MVP 완료는 다음 상황으로 정의한다.

사용자가 브라우저에 접속한다.

PNG 몬스터 이미지를 넣는다.

```text
몸통1
머리1
오른팔1
꼬리1
꼬리2
```

관절을 생성한다.

부모를 연결한다.

각 영역을 드래그 또는 브러시로 지정한다.

`대기` 버튼을 누른다.

캐릭터가 움직인다.

`공격` 버튼을 누른다.

캐릭터가 공격한다.

`피격` 버튼을 누른다.

캐릭터가 뒤로 튕긴다.

프로젝트를 저장한다.

Phaser Example 프로젝트에서 동일한 데이터를 불러온다.

```ts
monster.play("idle");
monster.play("attack");
monster.play("death");
```

가 정상적으로 실행된다.

**여기까지 되면 MVP 성공이다.**

---

# 70. Codex 작업 지침

처음부터 모든 기능을 한 번에 구현하지 않는다.

다음 순서대로 구현한다.

## Phase 1

프로젝트 초기화.

```text
Vite
TypeScript
Phaser
HTML/CSS
```

한국어 기본 UI 구축.

---

## Phase 2

PNG 업로드 및 Canvas 표시.

Zoom.

Pan.

---

## Phase 3

Bone 시스템.

```text
생성
선택
이동
삭제
부모 관계
계층 UI
```

---

## Phase 4

Mesh 생성.

PNG Texture를 Grid Mesh에 표시.

Bone Transform으로 Mesh Vertex가 이동할 기반 구축.

---

## Phase 5

Weight 시스템.

```text
원형 Influence
Soft Falloff
Weight Normalization
Weight Brush
Erase
```

---

## Phase 6

Animation Runtime.

```text
Transform Track
Interpolation
Loop
Events
```

---

## Phase 7

Preset 시스템.

```text
Idle
Attack
Hit
Death
Jump
```

---

## Phase 8

프로젝트 저장/불러오기.

```text
JSON
PNG
ZIP
```

---

## Phase 9

runtime-core 분리.

Editor 없이 Animation 실행 가능하게 한다.

---

## Phase 10

runtime-phaser 제작.

Phaser Example Scene 생성.

---

## Phase 11

Vercel 배포 설정.

GitHub push 시 자동 배포.

---

# 71. Codex가 매 단계에서 해야 할 것

각 Phase를 완료할 때:

1. TypeScript 빌드 오류가 없는지 확인한다.
2. 기존 기능이 깨지지 않았는지 확인한다.
3. 간단한 테스트를 추가한다.
4. README를 업데이트한다.
5. 다음 Phase에 필요한 구조를 과도하게 미리 구현하지 않는다.
6. 필요 이상으로 의존성을 추가하지 않는다.

---

# 72. UX 원칙

전문가용 툴처럼 보일 필요 없다.

사용자가 자신만 쓰기 편하면 된다.

따라서:

```text
작은 버튼
명확한 한국어
즉각적인 결과
적은 설정
많은 자동값
```

을 우선한다.

---

# 73. 자동값 우선

가능한 것은 자동으로 결정한다.

예:

```text
Mesh 크기

Weight falloff

Animation strength

Bone index

Tag index

프로젝트 파일명
```

사용자가 반드시 결정해야 하는 것만 UI로 노출한다.

---

# 74. UI 디자인 원칙

게임 엔진 스타일의 어두운 편집기 UI.

복잡한 장식은 필요 없다.

중요한 것은:

```text
캔버스가 가장 크게 보일 것

현재 선택된 Bone이 명확할 것

Weight가 색/투명도로 보일 것

부모 연결이 선으로 보일 것

미리보기 버튼이 항상 보일 것
```

---

# 75. 가장 중요한 제품 철학

이 프로그램의 목적은:

**잘 만든 애니메이션을 만드는 것보다, 많은 캐릭터에 빠르게 충분히 괜찮은 움직임을 부여하는 것이다.**

조금 찌그러지는 것은 허용한다.

조금 어색한 것도 허용한다.

오히려 종이인형 특유의 움직임을 스타일로 인정한다.

대신:

```text
빠르고

가볍고

범용적이고

게임에 바로 사용할 수 있어야 한다.
```

---

# 76. 최종 결과물

프로젝트 완성 시 저장소에는 최소 다음이 존재해야 한다.

```text
1. 웹 Puppet Editor

2. Puppet JSON Format 문서

3. runtime-core

4. runtime-phaser

5. 기본 Animation Preset

6. Phaser Demo

7. Sample Character

8. Vercel Deployment

9. 한국어 README
```

---

# 77. 최우선 순위

개발 중 선택이 필요할 경우 다음 순서를 따른다.

```text
1. 브라우저에서 안정적으로 실행

2. 빠른 캐릭터 세팅

3. 가벼운 Runtime

4. 임의 Skeleton 지원

5. Phaser 사용성

6. Animation 확장성

7. 시각적 품질

8. 전문 편집 기능

9. Unity / Unreal 지원
```

Unity와 Unreal을 위해 웹 버전을 복잡하게 만들지 않는다.

---

# 78. 첫 구현 작업

지금 바로 다음 작업부터 시작한다.

1. Vite + TypeScript 프로젝트를 생성한다.
2. Phaser를 설치한다.
3. 편집기 기본 레이아웃을 한국어로 만든다.
4. 중앙에 Phaser 기반 작업 캔버스를 만든다.
5. PNG를 드래그 앤 드롭하여 표시할 수 있게 한다.
6. 마우스 휠 Zoom과 우클릭 또는 중클릭 Pan을 구현한다.
7. 좌측에 `관절` 패널을 만든다.
8. `관절 추가` 버튼으로 이미지 위에 Bone Point를 생성할 수 있게 한다.
9. Bone 데이터를 TypeScript interface로 분리한다.
10. 이 단계가 완전히 작동한 후에만 부모 연결 시스템 구현으로 진행한다.

첫 구현부터 전체 Spine 대체 시스템을 만들려고 하지 말 것.

**가장 먼저 “이미지 위에 임의의 관절점을 만들고 저장할 수 있는 브라우저 툴”을 완성한다.**